# Baduk Assistant — Task Plan

Постоянный план проекта. Обновляется по ходу работы; читается в начале каждой сессии вместе с `findings.md` и `progress.md`.

## Постоянные правила (standing policies)

- **Git-ветки**: разработка каждой фазы/задачи ведётся в отдельной ветке, никогда не коммитим реализацию напрямую в `main`. Именование: `phase-N-<short-name>` для фаз roadmap ниже, `fix-<short-name>` для точечных правок. Перед началом работы — создать/переключиться на ветку и отметить задачу здесь как "in progress".
- **Восстановление контекста**: в конце каждой сессии — запись в `progress.md` (что сделано, что решено, что осталось открытым) и обновление статусов в этом файле. Новая сессия начинает с чтения этих трёх файлов, а не с расспросов заново.
- **Не начинать реализацию без явного запроса** — правило из `CLAUDE.md`: репозиторий на стадии «архитектура утверждена, реализация не начата», пока пользователь явно не попросит начать конкретную фазу.

## Где мы сейчас

**Фаза 1 (SGF viewer + KataGo-анализ, без LLM) реализована полностью — backend и frontend — и прошла ручную приёмку с живым KataGo, подтверждённую пользователем (2026-08-05).**
- Sidecar-скелет + `EngineManager`: health-check (FastAPI, токен-аутентификация), `KataGoProfile` + шаблон `.cfg`, `EngineManager` (жизненный цикл процесса KataGo Analysis Engine, авто-restart, дренаж stdout/stderr), реальный integration-тест против локального KataGo пользователя (winrate/ownership/PV подтверждены). Финальное ревью пройдено (7 Important-находок исправлены одним fix-wave).
- API-слой: `POST /api/analyze` + `WS /api/analyze/stream` поверх единственного `EngineManager`/`asyncio.Lock` в `app.state`. 5 задач + один fix-wave на 5 Important-находок финального ревью.
- Frontend (ветка `phase-1-frontend`, 11 задач через `subagent-driven-development`): Electron+TS+Preact-приложение с нуля — scaffold, backend-connection, SGF-парсинг+дерево (`@sabaki/sgf`+`@sabaki/immutable-gametree`), типизированный IPC-клиент, GTP-координатный request-builder, позиция на доске через `@sabaki/go-board`, состояние на `@preact/signals`, доска на `@sabaki/shudan`, дерево вариаций+клавиатурная навигация, ownership heatmap+PV-стрелки+winrate/score-lead график (`uPlot`), финальная интеграция (AppShell, drag&drop SGF, ConnectionGate, error-баннеры). Финальное whole-branch ревью (opus) нашло 7 Important-находок — 5 исправлены одним fix-wave.
- **Ветка `fix-phase-1-acceptance-findings`** (от `main`): реальная ручная приёмка с живым KataGo (`.katrain` opencl-сборка) нашла и исправила серьёзные баги, ни один из которых не всплыл ни на одном review — CSP (`connect-src`) блокировал вообще весь fetch/WS рендерера к backend (единственная причина сразу нескольких симптомов: WS-ошибка, пустые winrate/ownership/PV); неограниченный `ResizeObserver`-цикл в `BoardView` (тысячи `"loop completed"` в секунду, пеговал рендерер); дерево вариаций рисовалось диагональю вместо колонки (индент на каждый ход, а не только на ветвлении), буквенные подписи ходов заменены на точки, клавиши-стрелки требовали фокуса (стали глобальными), добавлена навигация колесом мыши; winrate-график: uPlot трактовал номера ходов как unix-время («1/1/70»), ось не ресайзилась при разворачивании окна, подписи осей рисуются на canvas и сливались с тёмной темой (дефолтный тёмно-серый stroke), курсор графика теперь синхронизирован с текущим ходом на доске (`setCursor`+`setLegend`, не только по наведению мыши); PV отображался как непонятные линии — заменено на пронумерованные маркеры топ-5 кандидатных ходов; добавлен маркер последнего хода, чекбоксы вкл/выкл heatmap и предлагаемых ходов; закрыт запаркованный WS `close`-listener. Диагностика велась через Chrome DevTools Protocol (`BADUK_DEBUG_PORT` env var, добавлен в `main/index.ts`) — реальные скриншоты и `Runtime.evaluate`/`Input.dispatch*` живого окна (доверенный ввод, не JS-синтетика), а не догадки по логам; это же выявило, что «двойная навигация» была ложной тревогой из-за счётчика DOM-класса `shudan-sign_N`, который Shudan ставит на 2 элемента на каждый камень. Все 33+46 тестов и typecheck зелёные на момент смёржа.

**Ветка `fix-phase-1-backlog-cleanup`** (от `main`): закрыт весь ранее отложенный backlog Фазы 1. Backend — `WebSocketDisconnect` теперь ловится в `/api/analyze/stream` (клиент может отключиться в любой момент многоходового стрима); `AnalyzeRequest`/`StreamAnalyzeRequest` переведены на общий `_BaseAnalyzeRequest` с границами значений (`maxVisits`, `komi`, `boardXSize`/`boardYSize`, длина `turnNumbers`); `main.py`'s `try/finally` расширен, чтобы покрывать `_find_free_port()`/`print()` — падение там больше не оставляет висящий временный `.cfg`-файл KataGo. Frontend — `mapSgfRules` теперь предупреждает в консоль на нераспознанном `RU`, а не тихо откатывается на chinese; `GTP_COLUMNS` вынесен в общий `board/gtpColumns.ts` (было продублировано в `BoardView.tsx` и `gameRequestBuilder.ts`); `getConnection()` в IPC-клиенте больше не кэширует отклонённый promise навсегда — следующий вызов ретраит; `sgfError`-сигнал в `App.tsx` экспортирован для сброса между тестами. Также найдена и закрыта отдельная, ранее не задокументированная проблема: `pnpm run lint` был красным почти целиком (1900+ из ~1950 предупреждений) из-за CRLF — эта машина использует `core.autocrlf=true`, а в репозитории не было `.gitattributes`, так что каждый чек-аут получал CRLF, а Prettier по умолчанию ожидает LF; добавлен корневой `.gitattributes` (`* text=auto eol=lf`) и рабочее дерево ренормализовано. Оставшиеся ~35 настоящих ошибок (`explicit-function-return-type`/`no-explicit-any` и т.п.) исправлены точечно в `src/` и ослаблены в `eslint.config.mjs` только для `tests/**` (моки/заглушки там осознанно используют `any`/пустые функции). `pnpm run lint` теперь полностью зелёный (0 ошибок, 0 предупреждений). Все 46 frontend + 42 backend теста и оба typecheck зелёные.

**Фаза 2 (LLM-объяснения поверх KataGo, без RAG), первый срез — реализован.** Пользователь явно попросил начать Фазу 2 (2026-08-05); брейнсторминг сузил весь объём Фазы 2 из ARCHITECTURE.md (несколько детекторов + calibration harness + 3 LLM-провайдера) до минимального среза по решению пользователя — только `weak_group`-детектор, только Claude, без калибровки, без свободных вопросов к LLM. Дизайн-спек: `docs/superpowers/specs/2026-08-05-phase-2-weak-group-explanation-design.md`. Implementation-план (8 задач): `docs/superpowers/plans/2026-08-05-phase-2-weak-group-explanation.md`, выполнен через `subagent-driven-development` на ветке `phase-2-llm-explanations`: backend — восстановление доски (`board/groups.py`+`board_state.py`+`gtp_coords.py`), детектор `weak_group` (`feature_extraction/`), LLM-схемы+orchestrator+consistency checker (`llm/`), Claude-провайдер (forced tool-use structured output), `POST /api/explain`; frontend — `explainPosition()` IPC-клиент, переключатель вкладок «KataGo/LLM» в панели анализа (`AnalysisPanel`+`LlmExplanationPanel`). Task 8 прошёл 2 раунда фиксов (утечка stale-результата при смене позиции + гонка с in-flight запросом) — оба переверифицированы re-review. Финальное whole-branch ревью (opus) нашло 1 Critical (async-хендлер `/api/explain` синхронно блокировал event loop на вызове Claude — замораживал и `/api/analyze/stream`) + 6 Important (риск обрезки ответа Claude из-за adaptive thinking + `max_tokens=1024`; `Finding` без цвета группы/человекочитаемых GTP-координат — объяснение было не привязано к доске; `uv.lock` не пересобран под `anthropic`; `BADUK_CLAUDE_API_KEY`/`MODEL` не задокументированы; `pnpm lint` перестал быть чистым; `verified`-статус anti-hallucination нигде не показывался в UI) — все исправлены одним fix-wave и переверифицированы, кроме `uv.lock` (см. backlog, `uv` недоступен в этой среде). Все 73 backend + 57 frontend теста, typecheck, lint зелёные на итоговом коммите. Ветка смёржена в `main` (fast-forward `d515c8d..2d23e52`) и запушена в `origin` (2026-08-06).

**Ручная SGF-разметка/комментарии — реализована, готова к ручной приёмке (не смёржена).** Пользователь явно попросил начать эту фичу 2026-08-06 (backlog-пункт, отложенный во время брейнсторминга Фазы 2). Не привязано к roadmap-фазам — расширение Фазы 1 (viewer→editor). Дизайн-спек: `docs/superpowers/specs/2026-08-06-manual-sgf-annotation-design.md`. Implementation-план (9 задач): `docs/superpowers/plans/2026-08-06-manual-sgf-annotation.md`, выполнен через `subagent-driven-development` на ветке `feature-sgf-annotation` (от `main`, форк-коммит `d8c722b`): backend — новый main-process модуль `frontend/src/main/fileIO.ts` (`saveFile`/`saveFileAs`/`promptUnsavedChangesChoice`, атомарная запись через temp+rename) + IPC-обвязка в `main/index.ts`/`preload/index.ts`; frontend — `board/annotations.ts` (мутация/чтение TR/SQ/CR/MA/LB/C через `tree.mutate()`, единственный источник истины — дерево партии), `board/sgfSerializer.ts` (`sgf.stringify([tree.root])` — сериализация обратно в файл), новые сигналы `currentFilePath`/`isDirty`/`currentNode` в `appState.ts`, тулбар разметки + клик-по-доске в `BoardView.tsx`, вкладка «Разметка» с комментарием узла (`AnnotationPanel.tsx`), UI сохранения в шапке `App.tsx`, предупреждение при закрытии окна с несохранёнными изменениями. Три раунда точечных фиксов на задачах 2/5/6 (все — реальные баги в самом плане, не в реализации: `sgf.stringify(tree.root)` без обёртки в массив давал невалидный SGF без внешних скобок; тулбар разметки рендерился даже без загруженной партии, позволяя «вооружить» инструмент заранее; `handleBlur` коммитил изменение в дерево на каждый выход из поля комментария, даже без правки) — каждый раз находка от ревьюера подтверждалась пользователем перед фиксом, т.к. противоречила буквальному тексту плана. Финальное whole-branch ревью (opus) нашло 1 Critical (lint не проходил — 2 реальные ESLint-ошибки + 15 предупреждений форматирования, накопленные из задач 5/7/8) + 5 Important (комментарий узла «залипал» после загрузки второго файла — `parseSgf` сбрасывает счётчик id на каждый разбор, поэтому корневые узлы разных партий получают одинаковый `id`; текст комментария терялся при навигации колёсиком мыши по дереву без потери фокуса; краш рендера на разметке с координатами вне доски из чужого SGF; перезапись файла в не-UTF8 кодировке могла молча испортить партию — по решению пользователя теперь предупреждает и отказывает; неатомарная запись файла; выход через Cmd+Q убивал backend раньше, чем пользователь успевал отменить диалог о несохранённых изменениях) + 8 Minor — все 17 находок исправлены одним consolidated fix-wave и переверифицированы scoped re-review (все ADDRESSED, один запаркован — осиротевший `.tmp`-файл при неудачном `rename()`, не блокирует). Все 18 файлов / 118 тестов, typecheck, `pnpm run lint` (0 ошибок, 0 предупреждений) зелёные на итоговом коммите `cdc5c43`.

**Ручная сквозная приёмка проведена и подтверждена пользователем 2026-08-06.** Ветка смёржена в `main` (fast-forward `d8c722b..40aeef1`). Не запушена в `origin` — отдельное решение, ещё не принято.

**Индикатор комментария/разметки в дереве вариаций — реализован, готов к мержу (не смёржен).** Небольшое UI-улучшение, предложенное пользователем сразу после приёмки SGF-разметки: маленький зелёный треугольник внутри маркера узла дерева вариаций, если у узла есть комментарий и/или разметка (`C`/`TR`/`SQ`/`CR`/`MA`/`LB`) — работает для любого узла, включая корень. Дизайн-спек: `docs/superpowers/specs/2026-08-06-variation-tree-annotation-indicator-design.md`. Implementation-план (1 задача): `docs/superpowers/plans/2026-08-06-variation-tree-annotation-indicator.md`, выполнен через `subagent-driven-development` на ветке `fix-variation-tree-annotation-indicator` (от `main`, форк `c76b360`): чистая функция `hasAnnotation(node)` + рендер SVG-треугольника внутри `.variation-tree__marker` в `VariationTree.tsx`, новый CSS-класс `.variation-tree__marker-annotation-icon`. Финальное whole-branch ревью (opus) нашло 2 Important (тесты не были привязаны к конкретному узлу — баг «иконка не на том узле» прошёл бы незамеченным; не было теста на корневой узел, хотя спек явно требует поддержки root) + 4 Minor (дублирование списка ключей разметки между `annotations.ts` и `VariationTree.tsx`; смещение иконки на ~1px от центра — но точные пиксельные значения взяты дословно из плана, поэтому правка требовала бы отступить от буквального текста плана; отсутствие a11y-подписи; не обновлённые `task_plan.md`/`progress.md`). По решению пользователя один фикс-вейв закрыл оба Important + дедупликацию ключей разметки (экспорт `FIGURE_PROPERTIES` из `annotations.ts`); смещение иконки и a11y-подпись осознанно оставлены как есть. Re-review подтвердил все 3 находки ADDRESSED, новых находок нет. Все 123 теста, оба typecheck, lint — зелёные на итоговом коммите `58e905f`.

**Gemini как второй LLM-провайдер — реализован, готов к мержу (не смёржен).** Пользователь явно попросил добавить Google Gemini (Google AI Studio) как альтернативу Claude — мотивация: нет ключа Claude API, но есть доступ к Gemini. Дизайн-спек: `docs/superpowers/specs/2026-08-06-gemini-llm-provider-design.md` (рассмотрена и отклонена унификация через LiteLLM — оставлены два отдельных провайдера на официальных SDK, т.к. forced tool-use — основа anti-hallucination дизайна, и добавлять слой трансляции между кодом и реальным API-контрактом рискованно; провайдеров ровно два, третий не планируется — YAGNI). Implementation-план (3 задачи): `docs/superpowers/plans/2026-08-06-gemini-llm-provider.md`, выполнен через `subagent-driven-development` на ветке `phase-2-gemini-provider` (от `main`, форк `7a62cb5`): Task 1 — вынесение `SYSTEM_PROMPT`/`build_user_prompt`/`EXPLANATION_TOOL_*` из `claude.py` в общий `llm/prompts.py` (существующие тесты Claude — неизменная регрессионная сеть); Task 2 — `GeminiProvider` (`llm/providers/gemini.py`) через официальный `google-genai` SDK, forced function-calling (`tool_config.mode="ANY"`) — Gemini-эквивалент Claude-эквивалента `tool_choice`; Task 3 — `_select_llm_provider()` в `main.py` по `BADUK_LLM_PROVIDER` (дефолт на момент этой фичи — `"gemini"`, намеренное решение пользователя, не `"claude"`; дефолт сменился на `"llama"` позже в этой же сессии, см. ниже), README/`CLAUDE.md` обновлены. Финальное whole-branch ревью (opus) нашло 1 Critical (`thinking_budget=0` — параметр линейки Gemini 2.5, а дефолтная модель `gemini-3.6-flash` — линейка Gemini 3, которой нужен `thinking_level` — риск 400-ошибки на каждом вызове дефолтного провайдера; независимо перепроверено через веб-поиск и интроспекцию установленного SDK) + 2 Important (устаревший `CLAUDE.md` всё ещё требовал `BADUK_CLAUDE_API_KEY` безусловно; обязательная по плану ручная проверка на живом API не была выполнена) + 4 Minor. Один фикс-вейв закрыл Critical (`thinking_level="MINIMAL"`) + `CLAUDE.md` + один Minor (`call.args=None` → `ValidationError` вместо `RuntimeError`) по выбору пользователя; `uv.lock`/`max_output_tokens`/`vertexai=False` осознанно отложены. Re-review подтвердил все 3 находки ADDRESSED. Живая проверка с реальным пользовательским `BADUK_GEMINI_API_KEY` дошла до Gemini API и получила валидный структурированный ответ — но `400 FAILED_PRECONDITION: User location is not supported` (гео-ограничение аккаунта/сети, не связано с кодом) вместо успеха; полный успешный round-trip не подтверждён. Пользователь явно одобрил мерж без полного round-trip. Все 85 backend-тестов, полный non-integration suite — зелёные на итоговом коммите `4a6cfbb`.

**llama-cpp-python как третий LLM-провайдер — реализован, готов к мержу (не смёржен).** Пользователь после мержа Gemini явно поднял новый вопрос: платные облачные API с ограничениями по запросам, попросил спроектировать переход на локальный `llama-cpp-python`. Уточняющий вопрос «какие локальные модели подойдут» разобран отдельным веб-поиском (август 2026), рекомендация — `Qwen3-8B` GGUF Q4_K_M (~5 ГБ, `unsloth/Qwen3-8B-GGUF`) под NVIDIA-карту с ≤8 ГБ VRAM пользователя (та же, что уже гоняет KataGo). Дизайн-спек: `docs/superpowers/specs/2026-08-06-llama-cpp-provider-design.md` — третий провайдер наравне с Claude/Gemini (дефолт `BADUK_LLM_PROVIDER` на момент этой фичи не менялся, оставался `"gemini"`; сменился на `"llama"` отдельной точечной задачей позже в этой же сессии, см. ниже), grammar-constrained JSON (`response_format={"type":"json_object","schema":...}`) вместо forced tool-use — механически даже более строгая гарантия structured output, чем у Claude/Gemini. Точный API `llama-cpp-python` v0.3.34 лично верифицирован по исходникам на GitHub перед планом (не по памяти) — критично, т.к. библиотечные дефолты (`n_gpu_layers=0`, `n_ctx=512`) не подходят и должны переопределяться явно. Implementation-план (2 задачи): `docs/superpowers/plans/2026-08-06-llama-cpp-provider.md`, выполнен через `subagent-driven-development` на ветке `phase-2-llama-provider` (от `main`, форк `7ba80cd`): Task 1 — `LlamaProvider` (`llm/providers/llama.py`) + юнит-тесты на fake-объекте + самоскипающийся интеграционный тест; Task 2 — `_select_llm_provider()` третья ветка `"llama"`, README (рекомендуемая модель + установка CUDA-wheel + Windows long-path обходной путь, лично воспроизведённый в этой сессии). Task 2 прошёл 1 раунд фикса — тест падал в изоляции (`TypeError` от eager-вычисления аннотации `llama_cpp.Llama | None` при монкипатче до первого импорта модуля), исправлено `from __future__ import annotations` в `llama.py`, живо переверифицировано изоляцией теста. Финальное whole-branch ревью (opus) нашло 3 Important (жёсткая зависимость `llama-cpp-python` ломала установку/сбор тестов для Claude/Gemini-only пользователей — на этой же машине уже наблюдался Windows long-path build fail; общий `Llama`-инстанс не защищён локом при параллельных `/api/explain` — риск порчи состояния/segfault, в проекте уже есть прецедент `engine_lock` для KataGo, здесь не применён; README не предупреждал о конкуренции за VRAM с KataGo на той же 8-ГБ карте) + 4 Minor. Фикс-вейв закрыл все 3 Important + 3 из 4 Minor (README-имя модели, невнятная ошибка на мусор в `BADUK_LLAMA_N_GPU_LAYERS`, `finish_reason`+content excerpt в текст ошибки) по выбору пользователя. Re-review нашёл residual gap в Important 1 (`main.py`'s `"llama"`-ветка всё ещё импортировала `LlamaProvider` до проверки `BADUK_LLAMA_MODEL_PATH`) — отступление от буквального текста плана (import-then-check скопирован из claude/gemini-веток), подтверждено пользователем, исправлено перестановкой порядка, живо переверифицировано повторным ре-ревью. **Живая проверка с реальной моделью Qwen3-8B-Q4_K_M.gguf (путь дал пользователь) прошла успешно** — `test_explain_with_real_llama` PASSED за 123с на CPU-инференсе (CUDA-сборка не загрузилась на этой машине — нет CUDA Toolkit runtime, только драйвер), непустой schema-конформный `Explanation` — первый полный успешный round-trip среди всех трёх провайдеров в этой сессии (в отличие от Gemini, упёршегося в гео-ограничение). Все 97 backend-тестов, полный non-integration suite — зелёные на итоговом коммите `b54ee4f`.

**Фаза 3 (RAG ingestion + retrieval, первый под-этап) — реализован, смёржен в `main`, живая проверка пройдена.** Пользователь явно попросил начать Фазу 3 (2026-08-11). Контент — не сбор с нуля из открытых источников, а готовый личный репозиторий пользователя `Baduk-knowledge-base` (452 карточки Принцип/Ошибка/Упражнение, реальные категории вместо иллюстративных из ARCHITECTURE.md). Дизайн-спек: `docs/superpowers/specs/2026-08-11-phase-3-rag-ingestion-design.md`. Объём среза — только ingestion (карточки → BGE-M3 эмбеддинги на CPU → Chroma) + `retrieve_knowledge(query, top_k)`, без подключения к LLM-пайплайну/frontend. Implementation-план (3 задачи): `docs/superpowers/plans/2026-08-11-phase-3-rag-ingestion.md`, выполнен через `subagent-driven-development` на ветке `phase-3-rag-ingestion` (от `main`, форк `70f7715`). Task 2 прошёл 1 раунд фикса (голый `except Exception` вокруг `delete_collection`; вырожденный тест «full rebuild», не отличавший полную пересборку от upsert-in-place) — оба резолвлены контроллером без вопроса пользователю. Финальное whole-branch ревью (opus): 0 Critical, 6 Important (все воспроизведены живьём, включая полный прогон парсера по всем 452 реальным карточкам), 10 Minor — 4 из 6 Important ревьюер прямо назвал дефектами самого плана (не имплементеров): `.gitignore` не покрывал `rag_store/` вопреки уже одобренному спеку; `test_cards.py` не имел `pytest.importorskip("yaml")` (ломал базовую установку без `[rag]`); эмбеддинг-модель не была явно закреплена на CPU; вырожденный ассерт (`len(snippets) <= 2`) в собственном тесте плана. Плюс: опечатка в `BADUK_KNOWLEDGE_BASE_PATH` молча уничтожала существующий индекс (`Path.glob()` на несуществующей директории просто возвращает `[]`); README не документировал новую env var/установку. Один консолидированный фикс-вейв (все 6 Important + 4 отобранных Minor) + scoped re-review (все 10 находок ADDRESSED, включая поведенческую проверку через monkeypatch). Ветка смёржена в `main` (fast-forward `70f7715..71171db`), тесты перепроверены на смёрженном дереве. **Живая проверка** с реальным `BADUK_KNOWLEDGE_BASE_PATH` (452 карточки `Baduk-knowledge-base`) — `test_ingest_and_retrieve_real_knowledge_base` PASSED за 27 минут (первая загрузка BGE-M3 + CPU-эмбеддинг всей базы); `retrieve_knowledge()` вернул `two-eyes-necessary-for-unconditional-life` в top-3 для известного запроса — критерий готовности спека подтверждён вживую. `backend/rag_store/` (~16 МБ) корректно гитигнорится. Все 131 backend-теста зелёные. Не запушено в `origin` (`main` на 12 коммитов впереди) — отдельное решение.

**Детектор `mistake` (второй под-этап Фазы 2) — реализован, финальное ревью + фикс-волна пройдены, готов к мержу (не смёржен).** Пользователь явно попросил продолжить «остаток Фазы 2», выбрал начать с доп. детекторов (не с калибровки/harness и не со свободных вопросов к LLM — те отложены как отдельные под-этапы). Дизайн-спек: `docs/superpowers/specs/2026-08-11-phase-2-mistake-detector-design.md`. Ключевые решения: формула `Δ` через разницу `rootInfo.scoreLead` до/после хода (не через `moveInfos`, которые могут не содержать сыгранный ход — тот путь молчаливо проваливался бы именно на самых грубых ошибках); знак — `mover_favorability = scoreLead if mover=="B" else -scoreLead`, опирается на уже зафиксированный в проекте `reportAnalysisWinratesAs=BLACK`; пороги (`0.5`/`1.5`/`6.0` очка) взяты не из иллюстративного примера ARCHITECTURE.md, а из реальной лестницы `eval_thresholds` инструмента KaTrain, явно помечены как некалиброванные под этот проект; при конфликте с `weak_group` побеждает `mistake` (по решению пользователя). Проверено и осознанно отклонено: репозиторий `Baduk-knowledge-base` (152 карточки Go-принципов/ошибок пользователя) не содержит числовых порогов — не применим к калибровке детектора, зато прямое попадание в будущую Фазу 3 (RAG). Implementation-план (5 задач): `docs/superpowers/plans/2026-08-11-phase-2-mistake-detector.md`, выполнен через `subagent-driven-development` на ветке `phase-2-mistake-detector` (от `main`, форк `ab15c21`). Task 1 (discriminated union `Finding`) прошёл 1 раунд фикса — implementer пропустил один из пяти файлов, напрямую конструирующих `Finding(...)` (`test_api_explain_integration.py`), план ошибочно предполагал, что `Finding` используется только как type hint; найдено и исправлено, один Minor (методология не описана в scratch-репорте) адъюдицирован контроллером без второго раунда — суть независимо перепроверена ре-ревьюером. Task 4 — обнаружена и исправлена реальная арифметическая ошибка знака в собственной тестовой фикстуре плана (у автора этого плана), подтверждена ре-ревьюером вручную. Task 3 — implementer сам нашёл и корректно закрыл ещё один пробел плана (`llm/schemas.py`'s `CitedField` тоже требовал `"delta_score"`, вне заявленного списка файлов). Финальное whole-branch ревью (opus) нашло 2 Critical (общий на оба типа находок enum `cited_field` при per-type `_FINDING_FIELDS` — цитата модели «чужого» поля падала `AttributeError`→503, **регрессия на уже существующем `weak_group`-пути**, не только новый код; три плановых файла проекта не обновлялись по ходу ветки) + 4 Important (`MistakeFinding.turn_number` был смещён на 1 от того, как рендерился в тексте; `EXPLANATION_TOOL_DESCRIPTION` не обобщена; фронтендовый `Claim.cited_field` не синхронизирован с бэкендом; промпт `mistake` смешивал две системы отсчёта `scoreLead`/`delta_score` без пояснения — модель могла процитировать верное число с неверной атрибуцией стороне, что прошло бы численную проверку) + 5 Minor. Один фикс-вейв закрыл все находки кроме планинг-файлов (сделаны контроллером напрямую) и части Minor (отложены в backlog); scoped re-review подтвердил все 6 находок ADDRESSED независимой перепроверкой (свежий repro-скрипт для Critical-бага), новых регрессий нет. Все 122 backend + 125 frontend теста, оба typecheck — зелёные на итоговом коммите.

**Фаза 3, третий под-этап (RAG-цитаты в UI) — реализован, финальное ревью + фикс-волна пройдены, готов к мержу (не смёржен).** Пользователь явно попросил продолжить Фазу 3. Брейнсторминг выявил, что буквальная фраза «цитаты в UI» требует не только frontend-работы — backend отдавал только голый `rag_doc_id` (строку), недостаточную для карточки цитаты. Решение: небольшое backend-расширение (`get_snippet_by_id` — точечный lookup по id через `collection.get()`, не семантический поиск, без `embedding_model`; новое поле `ExplainResponse.citation`) + frontend (нативный `<details>/<summary>` в `LlmExplanationPanel`, секция не рендерится без цитаты). Дизайн-спек: `docs/superpowers/specs/2026-08-12-phase-3-rag-citations-ui-design.md`. Implementation-план (2 задачи): `docs/superpowers/plans/2026-08-12-phase-3-rag-citations-ui.md`, выполнен через `subagent-driven-development` на ветке `phase-3-rag-citations-ui` (от `main`, форк `f239bbe`). Task 1 (backend) implementer сам поймал и корректно исправил реальный кросс-задачный тестовый гэп: уже смёрженный `_rag_doc_id_valid` в `consistency.py` независимо перепроверяет `rag_doc_id` через настоящий `retrieve_knowledge()` против реального `backend/rag_store/` на этой машине — выдуманный `doc_id` в тесте не проходил бы эту проверку без дополнительного мока, что скрыто ломало бы тестируемый путь; независимо перепроверено контроллером и ре-ревьюером. Оба task-level ревью — ноль находок. Финальное whole-branch ревью (opus): 0 Critical, 3 Important, 6 Minor — два Important однозначно резолвлены (блок обогащения цитаты в `explain.py` не был защищён от непредугаданных ошибок Chroma — `chromadb.errors.InternalError` не `RuntimeError`, ничем не ловился, роняя готовый ответ в 500; у развёрнутой карточки цитаты не было `max-height`/`overflow-y` — на реальной карточке 3122 символа/64 строки это схлопывало ряд с доской в ноль, подтверждено живым замером на реальных данных, не предположением). Третий Important (`typecheck:web` не покрывает `frontend/tests/**`, из-за чего верификационный шаг плана был холостым) — project-wide tooling-гэп вне рамок этой фичи, отложен в backlog, не исправлен молча. Один фикс-вейв (оба Important + defensive `.get()` на метаданных карточки + новый тест на fallback-инвариант) + scoped re-review (оба ADDRESSED) — READY TO MERGE. Все 157 backend + 127 frontend теста, typecheck — зелёные на итоговом коммите.

**Фаза 3, второй под-этап (подключение RAG к LLM-пайплайну) — реализован, финальное ревью + фикс-волна пройдены, готов к мержу (не смёржен).** Пользователь явно попросил продолжить Фазу 3 после живой проверки первого под-этапа. Брейнсторминг существенно сузил объём относительно исходного видения `ARCHITECTURE.md`: только backend (без цитат в UI), только `llama-cpp-python`-провайдер (Claude/Gemini не трогаются), agentic-решение «искать/не искать» с детерминированным query (не модель придумывает текст запроса), `oneOf`-JSON-схема с максимум одним хопом поиска на вызов, `rag_doc_id` на уровне `Explanation` с anti-hallucination проверкой через независимый пересчёт в `consistency.py` сразу (не отложено). Технический риск (поддержка `oneOf`/`const` в grammar-конвертере llama.cpp/llama-cpp-python) лично проверен по исходникам перед дизайном. Дизайн-спек: `docs/superpowers/specs/2026-08-12-phase-3-rag-llm-wiring-design.md`. Implementation-план (4 задачи): `docs/superpowers/plans/2026-08-12-phase-3-rag-llm-wiring.md`, выполнен через `subagent-driven-development` на ветке `phase-3-rag-llm-wiring` (от `main`, форк `4356de0`). Task 3 (agentic-поток в `llama.py`, самая сложная задача) прошёл 1 раунд фикса (потерян `finish_reason` в одной из веток сообщения об ошибке) — резолвлено контроллером без вопроса пользователю; имплементер также сам поймал и исправил реальный баг в собственном тестовом коде плана (порядок `monkeypatch`/autouse-фикстуры), независимо перепроверено контроллером перед ревью. Финальное whole-branch ревью (opus): 0 Critical, 4 Important, 4 Minor — 3 из 4 Important ревьюер прямо назвал дефектами спека, не имплементации: спек оценил стоимость дублирующего вызова `retrieve_knowledge` в «миллисекунды», реальный замер показал 7.5–14.4с на незакэшированную загрузку BGE-M3 (до 8 полных загрузок модели на один `/api/explain`-запрос без кэша); `top_k` для `retrieve_knowledge` был продублирован как отдельный литерал в `llama.py` и `consistency.py` вместо общего источника истины; ни один промпт не инструктировал модель реально заполнять `rag_doc_id`, из-за чего вся anti-hallucination механика цитат редко бы срабатывала. Плюс отдельный (implementation-level) Important — двойной вызов `_rag_doc_id_valid()` за одну итерацию retry-цикла. Один консолидированный фикс-вейв (все 4 Important + 2 Minor: `RAG_TOP_K` перенесён в общий `prompts.py`, эмбеддинг-модель закэширована через `functools.lru_cache`, инструкция по `rag_doc_id` добавлена в `_format_snippets()`, мёртвый `decision.pop("tool", None)` удалён) + scoped re-review (все 6 находок ADDRESSED, включая живой замер cache-identity) — READY TO MERGE. Все 150 backend-теста зелёные на итоговом коммите.

**Фаза 3, третий под-этап (RAG-цитаты в UI) — план готов, в работе.** Пользователь явно попросил продолжить Фазу 3. Брейнсторминг выявил, что buквальная фраза «цитаты в UI» требует не только frontend-работы — backend отдавал только голый `rag_doc_id` (строку), без title/source/text_snippet, недостаточных для карточки цитаты. Решено: небольшое backend-расширение (`get_snippet_by_id` — точечный lookup по id, не семантический поиск; новое поле `ExplainResponse.citation`) + frontend (нативный `<details>/<summary>` в `LlmExplanationPanel`, секция не рендерится без цитаты). Дизайн-спек: `docs/superpowers/specs/2026-08-12-phase-3-rag-citations-ui-design.md`. Implementation-план (2 задачи): `docs/superpowers/plans/2026-08-12-phase-3-rag-citations-ui.md`, ветка `phase-3-rag-citations-ui` (от `main`, форк `9606a7c`).

**Детектор `opening_loss` (третий под-этап остатка Фазы 2) — реализован, финальное ревью + фикс-волны пройдены, готов к мержу (не смёржен).** Пользователь явно попросил продолжить «остаток Фазы 2» и выбрал `opening_loss` (не калибровку/harness, не свободные вопросы к LLM). Δ переиспользует формулу `mistake` через общий `feature_extraction/scoring.py`; окно дебюта — существующая `K_OPEN` (не отдельный N); фронтенд собирает компактную последовательность из уже закэшированного `analysisByTurn`; накопленная потеря считается отдельно по цвету, пользователь выбирает цвет явно новой кнопкой «Проанализировать дебют»; отдельный эндпоинт `POST /api/explain/opening` (не расширение `/api/explain`); пороги (`3.0 < 5.0 < 15.0`) — иллюстративные, некалиброванные. Дизайн-спек: `docs/superpowers/specs/2026-08-15-phase-2-opening-loss-detector-design.md`. Implementation-план (6 задач): `docs/superpowers/plans/2026-08-15-phase-2-opening-loss-detector.md`, выполнен через `subagent-driven-development` на ветке `phase-2-opening-loss-detector` (от `main`, форк `0d642a8`). Task 6 прошёл 1 раунд task-level фикса (кнопка могла быть активна, но клик молча ничего не делал без анализа текущего узла). Финальное whole-branch ревью (opus): 0 Critical, 3 Important, 8 Minor — анти-регрессионное требование плана (exhaustive `match` вместо двухветочного `if/else`, тот класс бага, что уже дважды всплывал в истории проекта) независимо перепроверено ревьюером по всему репозиторию через grep — чисто. Все 3 Important — дефекты плана (буквальный код-скетч Task 6 скопирован имплементером один в один), не имплементации: `analysisAtEnd` в запросе брался из анализа текущего узла вместо границы дебютного окна (риск для anti-hallucination гарантии — LLM/checker могли увидеть/подтвердить числа чужой позиции); блок анализа дебюта не сбрасывался при загрузке другой партии; задизейбленная кнопка не показывала подсказку вопреки спеку. Один консолидированный фикс-вейв закрыл все 3; re-review находки сброса на новой партии выявил, что сам фикс (сброс по ссылочному равенству `tree`) внёс новую регрессию — `currentTree.value` пересоздаётся не только при загрузке файла, но и при любой разметке/правке комментария на той же партии, стирая результат анализа дебюта прямо во время обычной работы. По правилам скила «no second fix wave» для финального ревью находка не пошла в очередной автоматический раунд — вынесена пользователю явно с рекомендацией; пользователь выбрал исправить сейчас. Отдельный явный фикс-раунд (новый сигнал `gameLoadSequence`, инкрементируется только в `App.tsx`'s `loadGame()`) закрыл и это, re-review — ADDRESSED, без новой поломки. Все 175 backend + 141 frontend теста, оба typecheck — зелёные на итоговом коммите.

**Калибровка/backtesting harness (четвёртый под-этап остатка Фазы 2) — реализован, финальное ревью + фикс-волна пройдены, смёржен в `main`.** Пользователь явно попросил продолжить «остаток Фазы 2» и выбрал калибровку (не свободные вопросы к LLM — единственный оставшийся под-этап). Scope этого среза (согласован явно): версионированный JSON-конфиг детекторов переносится сейчас, а не откладывается (`detector_config.v1.json` + `config_loader.py`, pydantic-модели, `k_open`/`k_end` — на верхнем уровне `DetectorConfig`, разделяются `mistake`/`opening_loss`/валидатором `api/schemas.py`); единственный источник ground truth в этом срезе — self-consistency proxy (быстрый vs глубокий `maxVisits`-прогон одной и той же позиции через `EngineManager`, те же детекторы с тем же кандидатным конфигом на обоих прогонах; tsumego-корпус и Player Passport как источники ground truth явно отложены); реальный SGF-корпус пользователя (108 партий, путь передан пользователем лично) читается только через новую переменную окружения `BADUK_CALIBRATION_GAMES_PATH` (никогда не хардкодится); harness сэмплирует подмножество партий/позиций (не гоняет весь корпус) для контроля стоимости; результаты KataGo кэшируются на диск (`backend/calibration_cache/`, добавлен в `.gitignore`) по ключу `(sgf_path, turn_number, max_visits)`, чтобы повторный прогон с другим кандидатным конфигом не требовал новых вызовов KataGo. Новая зависимость `sgfmill>=1.1.1` (SGF-парсинг на backend, которого раньше не было вообще) — точный API (`Sgf_game.from_bytes`, `get_setup_and_moves`, `sgfmill.common.format_vertex`, `Board.is_empty()`) лично проверен по официальной документации/исходникам/`help()` на установленном пакете, не по памяти; координаты sgfmill численно совпадают с проектным GTP. Обратная совместимость — жёсткое требование, выполнено: все три детектора получают новые параметры (`config`/`k_open`/`k_end`/`min_reliable_visits`) со значениями по умолчанию, равными текущим константам — ни один существующий вызов (`explain.py`/`explain_opening.py`/все существующие тесты трёх детекторов) не изменился (независимо перепроверено и task-level, и финальным ревью). Дизайн-спек: `docs/superpowers/specs/2026-08-15-phase-2-calibration-harness-design.md`. Implementation-план (6 задач): `docs/superpowers/plans/2026-08-15-phase-2-calibration-harness.md`, выполнен через `subagent-driven-development` на ветке `phase-2-calibration-harness` (от `main`, форк `a9fd9ba`; имплементеры — haiku, task-ревью — sonnet). Task 3 прошёл 1 раунд task-level фикса: ревьюер поймал риск обрыва процесса на записи дискового кэша (не-atomic `write_text`, необработанный `ValidationError` на чтении битого файла) — роняло бы весь многочасовой прогон на следующем обращении к той же позиции; исправлено atomic-записью (temp-файл + `os.replace`) + фоллбэком на re-fetch при повреждённом кэше, re-review — ADDRESSED. Финальное whole-branch ревью (opus): 0 Critical, 7 Important, ~9 Minor — ключевые инварианты (анти-регрессионный гейт, единый кандидат-конфиг на обоих self-consistency прогонах, единственное место `k_open`/`k_end`) независимо перепроверены ревьюером, держатся. Все 7 Important закрыты одним консолидированным фикс-вейвом (sonnet), согласно политике скила «одна фикс-волна на финальное ревью»: отсутствующие `pytest.importorskip("sgfmill")` в 5 новых тестовых файлах (ломало документированный `pytest -v` без экстры `calibration`); `load_game()` молча терял форовые камни (`AB`/`AW`) — на реальном личном корпусе кю-игрока это тихо портило бы Δ на форовых партиях, теперь явный `ValueError` + пропуск партии через уже существующий `_load_games_skipping_errors`; устаревший фронтендский комментарий про `K_OPEN`, ссылавшийся на удалённый `config.py`, плюс задокументированный риск рассинхронизации при переопределении `k_open` через новую `BADUK_DETECTOR_CONFIG_PATH`; CLI и обе новые env-переменные задокументированы в `backend/README.md`; добавлены тесты, доказывающие, что явный `config` реально меняет поведение всех трёх детекторов и что `BADUK_DETECTOR_CONFIG_PATH` реально переопределяет `DEFAULT_CONFIG`; одна ошибка/таймаут KataGo больше не роняет весь прогон (try/except+warning+continue на каждой позиции, таймаут `fetch_analysis` увеличен 30с→120с); `min_reliable_visits`/`max_liberties_norm` получили `Field(gt=0)` — невалидный конфиг падает при загрузке, а не `ZeroDivisionError`-ом внутри детектора. Re-review (sonnet) — все 9 находок (7 Important + 2 safety-net) ADDRESSED, новых Critical/Important в фикс-диффе не найдено. Все 217 backend-тестов зелёные (плюс 8 integration — самоскипаются без реального KataGo/корпуса). Смёржена в `main` (fast-forward), тесты перепроверены на смёрженном результате. Живой прогон на реальном KataGo + реальном 108-игровом корпусе пользователя проведён в этой же сессии после мержа — см. запись «Живая проверка calibration harness…» в разделе «Текущие задачи» ниже.

**Свободные вопросы к LLM (последний под-этап остатка Фазы 2) — дизайн-спек и implementation-план готовы, реализация начата.** Пользователь явно попросил продолжить «остаток Фазы 2» и завершить её последним оставшимся под-этапом. В `docs/ARCHITECTURE.md` архитектурного видения под этим названием не было — введено пользователем при исходной декомпозиции Фазы 2, брейнсторминг этой сессии установил scope и архитектуру с нуля. Ключевые решения: чат про текущую позицию/партию (не общие вопросы про Го вне контекста партии); одноразовый вопрос-ответ, без истории диалога (YAGNI для первого среза); тот же паттерн anti-hallucination, что и у трёх существующих сценариев (structured-output + claims + пост-хок числовая проверка), но НЕ переиспользующий существующие `Claim`/`Explanation`/`consistency.py`'s finding-специфичную логику напрямую — параллельный, изолированный набор типов (`QuestionClaim`/`QuestionAnswer`, claims цитируют `rootInfo` или конкретный ход-кандидат из `moveInfos` через новое поле `cited_move`, не `finding_id`); только провайдер `llama` в этом срезе (эндпоинт возвращает `503` для claude/gemini, `LLMProvider`-протокол не трогается); RAG включён, LLM сама решает искать ли (agentic-паттерн, уже есть в `llama.py`); UI — поле ввода внутри существующей `LlmExplanationPanel.tsx`. Новый эндпоинт `POST /api/ask`. Дизайн-спек: `docs/superpowers/specs/2026-08-18-phase-2-free-form-questions-design.md`. Implementation-план (5 задач): `docs/superpowers/plans/2026-08-18-phase-2-free-form-questions.md` (само-ревью плана нашло и поправило 4 реальных несоответствия при сверке с актуальным кодом: `QuestionClaim.cited_field` должен быть типизированным `Literal`-алиасом, как у существующего `Claim.cited_field`, а не голым `str`; `analysis.moveInfos[i].move` уже в GTP-формате из KataGo, `xy_to_gtp`-конвертация не нужна вообще; эндпоинт должен проверять `hasattr(provider, "answer_question")`, а не `isinstance(provider, LlamaProvider)` — импорт конкретного класса потянул бы безусловный `import llama_cpp` в код, который обязан работать и с claude/gemini без установленного `llama_cpp`; `get_llm_provider` должен переиспользоваться из `explain.py` через импорт, как уже делает `explain_opening.py`, а не определяться заново). Работа ведётся через `subagent-driven-development` на ветке `phase-2-free-form-questions` (от `main`, форк `4d67e14`).

## Roadmap (из `docs/ARCHITECTURE.md` → «Поэтапный MVP-roadmap»)

| # | Фаза | Статус |
|---|------|--------|
| 1 | SGF viewer + KataGo-анализ (без LLM) | **полностью в `main`** (backend + frontend) |
| 2 | LLM-объяснения поверх KataGo (без RAG) | **первый срез (`weak_group`), Gemini, llama-cpp-python, детекторы `mistake`+`opening_loss`, calibration/backtesting harness — все в `main`** (llama-cpp-python — дефолтный провайдер, единственный с подтверждённым полным round-trip на реальной модели); **свободные вопросы к LLM — в работе** (см. «Где мы сейчас») — последний под-этап остатка Фазы 2 |
| 3 | RAG-база знаний | **первый и второй под-этапы в `main` (только llama); третий (RAG-цитаты в UI) реализован, не смёржен** (см. «Где мы сейчас») — Claude/Gemini RAG-поддержка не начата |
| 4 | Паспорт игрока | не начата |
| 5 | Формализация плагинной системы | не начата |
| 6 | Кросс-платформенная валидация | не начата |

Детали каждой фазы, критические файлы и критерии проверки — см. `docs/ARCHITECTURE.md` (разделы «Поэтапный MVP-roadmap» и «Проверка»).

## Текущие задачи (current tasks)

- [x] Backend Фазы 1 (sidecar+EngineManager+API-слой) — реализован, смёржен в `main`.
- [x] Brainstorming frontend-части Фазы 1 — дизайн-спек `docs/superpowers/specs/2026-08-04-phase-1-frontend-design.md` (визуальное сравнение layout + текстовые вопросы: SQLite отложен, renderer владеет IPC напрямую, uPlot, layout-вариант B).
- [x] Детальный implementation-план frontend-части — `docs/superpowers/plans/2026-08-04-phase-1-frontend.md` (11 задач).
- [x] Выполнить план frontend-части через `subagent-driven-development` (11 задач + финальное ревью + один fix-wave на 5 из 7 Important-находок).
- [x] Смёржить ветку `phase-1-frontend` в `main` через `superpowers:finishing-a-development-branch` (fast-forward `b0107d9..568cfa7`, ветка и SDD-воркспейс удалены, тесты перепроверены на смёрженном `main`).
- [x] `streamAnalysis` WS `close`/`error` — закрыт в рамках fix-ветки.
- [x] **Ручная сквозная приёмка Фазы 1 — подтверждена пользователем 2026-08-05.** Несколько раундов реального тестирования нашли и закрыли реальные баги (CSP, ResizeObserver-цикл, диагональное дерево, фокус-зависимые стрелки, "Time"-ось графика, несинхронный курсор графика, нечитаемые подписи осей и т.д.) на ветке `fix-phase-1-acceptance-findings`.
- [x] Смёржить `fix-phase-1-acceptance-findings` в `main`.
- [x] Запушить `main` в `origin`.
- [x] **Разобрать backlog Фазы 1** (ветка `fix-phase-1-backlog-cleanup`): все ранее отложенные минорные находки backend + frontend закрыты — см. decisions log 2026-08-05.
- [x] **Фаза 2, первый срез (`weak_group`)** — дизайн, план, реализация (8 задач) и финальное ревью + fix-wave завершены на ветке `phase-2-llm-explanations` (2026-08-05/06).
- [x] Смёржить `phase-2-llm-explanations` в `main` (fast-forward `d515c8d..2d23e52`, ветка удалена) и запушить в `origin` (2026-08-06).
- [x] **Ручная SGF-разметка/комментарии** — дизайн, план, реализация (9 задач), финальное ревью + один consolidated fix-wave (17 находок) завершены на ветке `feature-sgf-annotation` (2026-08-06).
- [x] Смёржить `feature-sgf-annotation` в `main` (fast-forward `d8c722b..40aeef1`, ветка и SDD-воркспейс удалены, тесты перепроверены на смёрженном `main`) (2026-08-06).
- [x] **Ручная сквозная приёмка `feature-sgf-annotation` — подтверждена пользователем 2026-08-06.** Реальный Electron + реальный KataGo (`.katrain` opencl-сборка) + настоящий drag&drop через CDP (`Input.dispatchDragEvent` с реальным путём файла — не JS-Blob) + реальная запись на диск, проверены сценарии: открытие чужого SGF с разметкой/комментарием (включая пограничный случай «фигура+метка в одной точке»), расстановка новой разметки+правка комментария, «Сохранить»→файл на диске изменился→переоткрытие показывает те же изменения; нативные диалоги («Сохранить как», предупреждение при закрытии) проверены пользователем лично (CDP не может кликать нативные Windows-диалоги). Не запушено в `origin` — отдельное решение.
- [x] **Индикатор комментария/разметки в дереве вариаций** — дизайн, план, реализация (1 задача), финальное ревью + один фикс-вейв (2 Important + 1 Minor) завершены на ветке `fix-variation-tree-annotation-indicator` (2026-08-06). Готово к мержу — решение о мерже/пуше ещё не запрошено у пользователя.
- [x] **Gemini как второй LLM-провайдер** — дизайн, план, реализация (3 задачи), финальное ревью + один фикс-вейв (1 Critical + 1 Important + 1 Minor) завершены на ветке `phase-2-gemini-provider` (2026-08-06). Живая проверка с реальным ключом дошла до API, но упёрлась в гео-ограничение аккаунта (не код) — пользователь одобрил мерж без полного round-trip. Готово к мержу.
- [x] **llama-cpp-python как третий LLM-провайдер** — дизайн, план, реализация (2 задачи), финальное ревью + фикс-вейв в 2 раунда (3 Important + 3 Minor, плюс residual-gap фикс после re-review) завершены на ветке `phase-2-llama-provider` (2026-08-06). Живая проверка с реальной моделью Qwen3-8B-Q4_K_M.gguf прошла успешно (PASSED, CPU-инференс) — первый подтверждённый полный round-trip среди трёх провайдеров. Готово к мержу.
- [x] **Vulkan GPU-оффлоад для llama-cpp-python подтверждён живьём** (2026-08-11, ручная диагностика вне pytest) — CUDA-wheel по-прежнему не грузится на этой машине (нет CUDA Toolkit), но prebuilt Vulkan-wheel грузит все 37/37 слоёв на GPU (RTX 5060 Ti), ~29.7 ток/с против ~123с на CPU. README дополнен Vulkan-инструкцией как альтернативой CUDA (ветка `fix-llama-vulkan-docs`, смёржена в `main`).
- [x] **Детектор `mistake` (второй под-этап Фазы 2)** — дизайн, план, реализация (5 задач через `subagent-driven-development`), финальное whole-branch ревью (opus, 2 Critical + 4 Important + 5 Minor) + один фикс-вейв + scoped re-review (все 6 находок ADDRESSED) завершены на ветке `phase-2-mistake-detector` (2026-08-11). Готово к мержу — решение о мерже/пуше ещё не запрошено у пользователя.
- [x] **Фаза 3, первый под-этап (RAG ingestion + retrieval)** — дизайн, план, реализация (3 задачи через `subagent-driven-development`), финальное whole-branch ревью (opus, 6 Important + 10 Minor) + один консолидированный фикс-вейв завершены на ветке `phase-3-rag-ingestion`, смёржена в `main` (fast-forward `70f7715..71171db`) (2026-08-11). Живая проверка с реальным `BADUK_KNOWLEDGE_BASE_PATH` (452 карточки) — PASSED. Не запушено в `origin`.
- [x] **Фаза 3, третий под-этап (RAG-цитаты в UI)** — дизайн, план, реализация (2 задачи через `subagent-driven-development`), финальное whole-branch ревью (opus, 3 Important + 6 Minor) + один фикс-вейв (READY TO MERGE) завершены на ветке `phase-3-rag-citations-ui` (2026-08-12). Готово к мержу — решение о мерже/пуше ещё не запрошено у пользователя.
- [x] **Фаза 3, второй под-этап (подключение RAG к LLM-пайплайну, только llama)** — дизайн, план, реализация (4 задачи через `subagent-driven-development`), финальное whole-branch ревью (opus, 4 Important + 4 Minor) + один консолидированный фикс-вейв (READY TO MERGE) завершены на ветке `phase-3-rag-llm-wiring` (2026-08-12). Готово к мержу — решение о мерже/пуше ещё не запрошено у пользователя.
- [x] **Детектор `opening_loss` (третий под-этап остатка Фазы 2)** — дизайн, план (6 задач через `subagent-driven-development`), финальное whole-branch ревью (opus, 3 Important + 8 Minor) + 2 автоматических фикс-раунда + 1 явный пользовательский фикс-раунд (все re-review ADDRESSED) завершены на ветке `phase-2-opening-loss-detector` (2026-08-15). Смёржена в `main` (fast-forward `0d642a8..af4810d`) в той же сессии.
- [x] **Калибровка/backtesting harness (четвёртый под-этап остатка Фазы 2)** — дизайн, план (6 задач через `subagent-driven-development`), финальное whole-branch ревью (opus, 7 Important + ~9 Minor) + 1 task-level фикс-раунд (Task 3, дисковый кэш) + 1 консолидированный финальный фикс-вейв (все 9 находок re-review ADDRESSED) завершены на ветке `phase-2-calibration-harness` (2026-08-17). Смёржена в `main` (fast-forward) в той же сессии; тесты на смёрженном `main` перепроверены (217 backend + 141 frontend). Не запушено в `origin`.
- [x] **Живая проверка calibration harness на реальном KataGo + реальном корпусе (2026-08-17/18)** — пользователь дал `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL`/`BADUK_CALIBRATION_GAMES_PATH` (109 SGF-файлов); интеграционный тест PASSED вживую; пробный прогон (3 партии) и полный прогон по дефолтам CLI (20 партий) оба завершились успешно, обе защиты финальной фикс-волны (skip-on-handicap, skip-on-KataGo-error) сработали на реальных данных как спроектировано. Первый содержательный калибровочный сигнал: `weak_group`'s F1 просел на большей выборке (0.86→0.73) — не повод менять `threshold_weak` прямо сейчас, но первый реальный аргумент. Найдена и запаркована в backlog новая data-quality проблема двух партий корпуса (`KM[375]`, невалидная коми, похоже на артефакт экспорта `foxwq`).
- [ ] **Свободные вопросы к LLM (последний под-этап остатка Фазы 2)** — дизайн-спек + implementation-план (5 задач) готовы, реализация начата через `subagent-driven-development` на ветке `phase-2-free-form-questions` (от `main`, форк `4d67e14`) (2026-08-18). См. «Где мы сейчас».
- [x] **Живая проверка calibration harness на реальном KataGo + реальном корпусе — подтверждена (2026-08-17).** Пользователь дал `BADUK_KATAGO_BINARY` (OpenCL-сборка `katago-v1.16.0-opencl-windows-x64.exe`), `BADUK_KATAGO_MODEL` (`kata1-b28c512nbt-s12464049920-d5727206990.bin.gz`) и `BADUK_CALIBRATION_GAMES_PATH` (личный корпус, 109 SGF-файлов) — ни один путь не хардкодился, переданы только как env vars на момент запуска. Интеграционный тест (`test_harness_integration.py`, 1 партия, fast=5/deep=20) — PASSED вживую, оба критерия готовности из спека подтверждены реальным движком (таблица печатается без ошибок; второй прогон по тому же ключу не обращается к KataGo повторно — уже подтверждено раньше юнит-тестами дискового кэша, здесь довалидировано на реальных данных). Затем пробный прогон самого CLI (3 партии, шаг 10, fast=50/deep=300, seed=0) дал первые реальные числа на `detector_config.v1.json`: `weak_group` precision=0.78 recall=0.97 F1=0.86 (TP=28 FP=8 FN=1 TN=22); `mistake` precision=0.95 recall=0.97 F1=0.96 (TP=37 FP=2 FN=1 TN=19); `opening_loss` precision=1.00 recall=1.00 F1=1.00 (TP=6, остальное 0). Интерпретировать осторожно: это self-consistency proxy (устойчивость находок к visit-budget шуму KataGo, не сверка с истинной «правильностью» хода), выборка — 3 партии из 109, `seed=0` — не репрезентативно для реального вывода о качестве порогов; также напоминание про уже задокументированный backlog-пункт — `weak_group`'s recall в этой метрике систематически завышен (случай «нашли разные группы» считается только FP, никогда FN).

- [x] **Полноценный прогон calibration harness на дефолтных параметрах CLI — выполнен (2026-08-18).** 20 партий (`--games-sample` по умолчанию), шаг 5, fast=50/deep=500 visits, `seed=0`, те же реальные `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL`/`BADUK_CALIBRATION_GAMES_PATH`, что и в пробном прогоне (переиспользован дисковый кэш с пробного прогона). Итог на `detector_config.v1.json`: `weak_group` precision=0.65 recall=0.84 F1=0.73 (TP=203 FP=109 FN=39 TN=274); `mistake` precision=0.90 recall=0.96 F1=0.93 (TP=329 FP=36 FN=12 TN=248); `opening_loss` precision=1.00 recall=1.00 F1=1.00 (TP=31 FP=0 FN=0 TN=3). На большей и более разнообразной выборке `weak_group`'s F1 заметно просел относительно пробного прогона (0.86→0.73, precision 0.78→0.65) — детектор реже подтверждается на глубоком проходе, чем срабатывает на быстром; первый содержательный калибровочный сигнал этого среза, но пока не действие (нужна ещё большая выборка/несколько seed'ов, прежде чем менять `threshold_weak`).
  - **Обе задуманные фикс-волной защиты сработали вживую, как спроектировано, не только в тестах**: (1) одна партия (`ZJOHONSYMF.sgf`) содержала форовые камни (`AB`/`AW`) — пропущена целиком с явным warning ещё на этапе загрузки корпуса (Fix 2 финального ревью), прогон не упал; (2) 2 из 19 сэмплированных партий («…[lumion]vs[V197780941]…», «…[V188304711]vs[lumion]…», обе экспортированы приложением `foxwq`) заставляли реальный KataGo возвращать error-ответ на КАЖДОЙ сэмплированной позиции — все такие позиции пропущены по одной с warning'ом (Fix 6), не уронив весь получасовой прогон.
  - **Новая находка (не фикс харнесса, а data-quality проблема в двух конкретных SGF файлах корпуса, не исправлялась — по запросу не просили)**: у обеих проблемных партий `KM[375]` — комментарий на 375 очков коми, явно некорректное/нестандартное значение (типичная коми — 6.5/7.5), похоже на артефакт экспорта из `foxwq` (китайская Go-платформа), возможно масштабированное представление (375 → 3.75?). `AnalyzeRequest`'s собственный `Field(ge=-150, le=150)` на `komi` этот случай бы отловил на входе — но `calibration/cache.py`'s `fetch_analysis()` строит сырой dict-запрос в обход этой pydantic-модели (согласовано дизайном спека — харнесс работает напрямую с `EngineManager`, не через HTTP API), так что невалидная коми доходит прямо до реального KataGo-процесса, который сам отвечает error-объектом вместо `moveInfos`/`rootInfo`. Харнесс не падает (см. выше), но обе партии фактически не дали калибровочного сигнала. Backlog: либо провалидировать `komi` в `load_game()`/`CalibrationGame` на разумный диапазон и skip-with-warning как для фор (симметрично Fix 2), либо оставить как есть (текущее поведение уже не ломает прогон, просто тратит немного времени на заведомо бесполезные вызовы KataGo).

- **Фаза 3, второй под-этап (`phase-3-rag-llm-wiring`, финальное ревью): три отложенных Minor-находки.** (1) Нет тестового покрытия на деградацию, когда `[rag]`-экстра вообще не установлена (`ImportError`-ветка `_rag_available()`) — обе прямые проверки `_rag_available` начинаются с `pytest.importorskip("chromadb"/"sentence_transformers")`, так что на машине без экстры пропускаются, а на машине с экстрой эта ветка недостижима; сама ветка проверена вручную ревьюером и признана корректной. (2) `monkeypatch.undo()` в двух тестах `_rag_available` работает только потому, что autouse-фикстура `_no_rag_by_default` и тело теста шарят один и тот же `monkeypatch`-инстанс (function-scoped) — хрупко, если позже добавится ещё одна autouse-фикстура на patch другого имени, эти два теста молча снесут и её тоже; смягчено подробным inline-комментарием, объясняющим почему. (3) `_rag_available()` заново читает `DEFAULT_STORE_PATH` через локальный импорт при каждом вызове, а `retrieve_knowledge()`'s дефолт-параметр `store_path: Path = DEFAULT_STORE_PATH` связывается один раз при определении функции — тесты патчат первое, но не гарантируют, что второе видит тот же путь; сейчас безвредно (прод никогда не переопределяет путь), но неявное расхождение при будущей фиче явного override пути.

- **Фаза 3, третий под-этап (`phase-3-rag-citations-ui`, финальное ревью): отложенные находки.** (1) `frontend/tsconfig.web.json`'s `include` не покрывает `frontend/tests/**` — `pnpm run typecheck:web` никогда не проверяет типы тестовых файлов, из-за чего единственное место, где на фронтенде воспроизводится HTTP-контракт `/api/explain` (мок-литералы `ExplainResponse` в тестах), вообще вне системы типов; будущий дрейф контракта backend↔frontend этот гейт не поймает. Чинить либо добавлением `tests/**/*.ts(x)` в `include` (аккуратно — потянет типы vitest/testing-library), либо отдельным `tsconfig.test.json`+скриптом `typecheck:test` — project-wide tooling-решение, не специфичное для одной фичи, требует отдельного рассмотрения. (2) Двойной запрос к Chroma на одну процитированную карточку: `consistency.py`'s `_rag_doc_id_valid` уже получает полный `RagSnippet` через `retrieve_knowledge`, но использует только булев факт присутствия `doc_id` и выбрасывает остальные поля; `explain.py` затем идёт в Chroma повторно через `get_snippet_by_id` за теми же title/source/text_snippet. Дёшево (замерено ~3мс, эмбеддинг не считается повторно), но по существу дублирование; полноценное исправление требует трогать `consistency.py`, что явно вне объёма этого среза. (3) UX-полировка секции цитаты — сейчас нет подписи, что это цитата из базы знаний (просто заголовок+путь появляются под объяснением); `source` показывает внутренний путь репозитория (`principles/....md`) как есть; тело карточки — сырой Markdown, `#`/`##`-заголовки видны буквально при `white-space: pre-wrap`. Не исправлено сознательно — это реальные продуктовые решения (нужна ли подпись, скрывать ли путь, рендерить ли markdown), не решённые на брейнсторминге этого среза, а не баги.

- **Калибровка/backtesting harness (`phase-2-calibration-harness`, финальное ревью): отложенные Minor-находки.** (1) `classify_finding` для `weak_group`: если быстрый и глубокий проходы нашли *разные* группы, засчитывается только FP, никогда FN (см. `test_classify_finding_both_present_but_different_groups_is_fp_not_tp`) — этот случай выпадает из знаменателя recall, из-за чего печатаемый recall систематически завышен. Поведение соответствует плану, но это скорее спек-уровневое решение, чем баг реализации — не тронуто, требует отдельного явного решения при следующем касании метрик self-consistency. (2) `harness.py`'s `_build_engine_manager()` использует голый `os.environ[...]` (кидает `KeyError`) вместо `main.py`'s явного `RuntimeError` с понятным сообщением для `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL` — функционально работает (падает громко), но менее дружелюбно для офлайн dev-инструмента, которым пользуются вручную. (3) `render_analysis_config()`'s поля `rules`/`board_size`/`komi` в `KataGoProfile`, который собирает `_build_engine_manager()`, фактически не используются (реальные параметры на каждый запрос берутся из SGF-партии через `AnalyzeRequest`) — мёртвые, вводящие в заблуждение значения в профиле; безвредно. (4) Pydantic-модели конфига (`WeakGroupConfig`/`MistakeConfig`/`OpeningLossConfig`) мутабельны и раздаются как shared default-параметры детекторов (`config: WeakGroupConfig = DEFAULT_CONFIG.weak_group`, связывается один раз при импорте модуля) — сейчас безвредно (никто не мутирует такой объект на месте), но `model_config = ConfigDict(frozen=True)` убрало бы саму возможность такого класса багов, если код когда-нибудь начнёт мутировать переданный `config`. (5) `run_harness()` при отсутствующей директории корпуса печатает то же сообщение, что и для пустой директории («no .sgf files found under …») — не различает «путь не существует» и «путь существует, но пуст», спека просила различать; чисто UX-полировка сообщения об ошибке. (6) Живой прогон харнесса на реальном KataGo + реальном 108-партийном корпусе пользователя не проводился в этой сессии — интеграционный тест самоскипается без `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL`/`BADUK_CALIBRATION_GAMES_PATH`, весь путь обращения к KataGo проверен только моками. Требует отдельной сессии с реальными переменными окружения пользователя, до выпуска первого калиброванного `detector_config.v2.json`. (7) Из task-level ревью Task 1: `config: WeakGroupConfig = DEFAULT_CONFIG.weak_group` — тот же shared-mutable-default паттерн, что и (4) выше, отмечен ещё на уровне Task 1 как «footgun» для Task 5, но Task 5 передаёт `config` явно на каждый вызов (не полагается на дефолт) — риск не материализовался, но паттерн остался в сигнатурах трёх детекторов. (8) `test_load_detector_config_raises_on_missing_field` проверяет `pytest.raises(Exception)` вместо конкретного `pydantic.ValidationError` — слабее, чем могло бы быть, но это буквально прописанный в плане код теста, не выбор имплементера. (9) `load_game()` (Task 2, до находки #2 финального ревью, которая эту проблему уже закрыла для форовых партий) изначально отбрасывал форовые камни без предупреждения — исправлено в финальном фикс-вейве явным `ValueError`+skip, здесь упомянуто только для полноты истории решения.

## Будущие задачи (backlog)
- Проверить точечно совместимость нужных React-only библиотек (напр. shadcn/Radix) через `preact/compat`, если/когда они понадобятся во frontend-части Фазы 1 (см. `findings.md`).
- Ownership-heatmap на очень раннем/малопосещённом (`maxVisits`) анализе выглядит визуально «залитым» почти по всей доске одним цветом — не трогали намеренно: неясно, баг рендера или просто честное отражение шумной низковизитной оценки KataGo. Понаблюдать на партиях с бόльшим числом визитов/ходов, прежде чем что-то менять.
- Уточнить, почему `uv` пропал из PATH в части сессий (Task 1 backend использовал его успешно, поздние задачи — уже нет); README backend уже документирует рабочий fallback через `.venv` напрямую. Тот же `uv` недоступен и в сессии 2026-08-06 (проверено напрямую — не в PATH, не модуль в `.venv`, нет типичных путей установки) — из-за этого **`backend/uv.lock` не пересобран** после добавления зависимости `anthropic` в `pyproject.toml` (Фаза 2). Функциональность не страдает (`.venv` уже содержит `anthropic`), но чистый `uv sync --locked`/`--frozen` на другой машине упадёт. Нужно один раз запустить `uv lock`/`uv sync` там, где `uv` реально стоит, и закоммитить обновлённый `uv.lock`. Тот же пробел теперь и у `google-genai` (добавлена для Gemini-провайдера, 2026-08-06) и `llama-cpp-python` (добавлена для локального llama.cpp-провайдера, 2026-08-06) — `uv.lock` не отражает ни одну из трёх зависимостей.
- **Асимметричные доски и GTP-координаты в LLM-объяснении (Фаза 2, минорная находка финального ревью).** `backend/src/baduk_backend/api/explain.py` передаёт только `body.boardXSize` как единственный параметр `board_size` в `xy_to_gtp()` (вычисляет номер строки как `board_size - y`) — на теоретической не квадратной доске (`boardXSize != boardYSize`, что `ExplainRequest` сейчас не запрещает) LLM получит неверный номер строки в GTP-координатах находки. Не блокирующая находка: доски Го всегда квадратные на практике (9×9/13×13/19×19), ни один тест/код-путь не использует не квадратную доску. Почини либо явным запретом `boardXSize != boardYSize` в `ExplainRequest`, либо протаскиванием обоих измерений через `xy_to_gtp`/`_user_prompt`.
- **Утечка процессов от тестовой инфраструктуры (обнаружено 2026-08-09, ещё не починено — только вручную остановлено).** На машине разработки найдено 8 зависших процессов `python.exe` (backend-сервис `baduk-backend.exe`) и 69 зависших процессов `node.exe` (тестовый фикстур-сервер `frontend/tests/fixtures/backend/fake-backend.mjs`), стартовавших 4–6 августа и не завершившихся — судя по всему, backend-сервис/фикстур-сервер не убивается штатно при прерывании/завершении прогонов `pytest -m integration`/`vitest run`, а копится от сессии к сессии. Все были idle (0% CPU), KataGo среди них не было; вручную остановлены (`Stop-Process`). Стоит найти и починить причину (недостающий teardown/`afterAll` для дочернего процесса, либо процесс не привязан к жизненному циклу тестового раннера) — иначе утечка будет повторяться при каждом прогоне тестов.
- **Фаза 3 ingestion (`phase-3-rag-ingestion`, финальное ревью): четыре отложенных Minor-находки.** (1) `get_embedding_model()` не кэширован (`@lru_cache`) — сейчас безвредно, т.к. вызывается вручную из CLI-скрипта, но актуальным станет, когда `retrieve_knowledge` начнёт вызываться из `/api/explain` на каждый запрос (следующий под-этап Фазы 3) — тогда без кэша каждый запрос будет грузить ~2 ГБ модели заново. (2) Нет предупреждения в лог для карточек длиннее контекстного окна BGE-M3 (8192 токена) — молчаливое усечение эмбеддинга у самой длинной карточки реальной базы (~27К символов, `vital-point-decides-limited-eyespace-life`), затрагивает ровно 1 карточку из 452, на `text_snippet` не влияет (там полное тело). (3) Коллизия `doc_id` между `principles`/`mistakes`/`exercises` не детектируется явно (`doc_id = path.stem`, три каталога сливаются в один `collection.add`) — коллизий в реальной базе сейчас нет, риск латентный. (4) Описание pytest-маркера `integration` в `backend/pyproject.toml` устарело («requires a real KataGo binary/model») — уже покрывает три LLM-провайдера и RAG, унаследовано от Фазы 2.
- **Детектор `mistake` (`phase-2-mistake-detector`, финальное ревью): три отложенных Minor-находки.** (1) Нет API-теста на «`analysisAfter`/`nextMove` переданы, но `Δ` ниже порога → корректный fallback на `weak_group`» — путь `mistake_finding or weak_finding` сейчас проверен только для случая отсутствующих полей, не для «переданы, но не сработали». (2) Пара-валидатор `ExplainRequest` (`analysisAfter`/`nextMove` только вместе) протестирована только в одном направлении (`analysisAfter` без `nextMove`), не в обратном. (3) Список допустимых значений `cited_field` теперь дублируется в трёх местах без единой синхронизации (`llm/prompts.py`'s enum, `llm/schemas.py`'s `CitedField` Literal, `ipc/client.ts`'s `Claim.cited_field` union) — финальное ревью уже поймало один реальный рассинхрон между ними (фронтенд отстал), дешёвая страховка — тест, что все три списка равны.
- **Детектор `opening_loss` (`phase-2-opening-loss-detector`, финальное ревью): 8 отложенных Minor-находки + 1 к рассмотрению.** (1) Нет теста на точную границу `severity="medium"` (`delta_score == OPENING_LOSS_SEVERITY_MEDIUM == 5.0`). (2) `nodeIdsFromRootToNode` (frontend) дублирует цикл обхода `movesFromRootToNode` практически один в один — можно переписать второе через первое. (3) Формула `window_end` продублирована между валидатором `api/schemas.py` и детектором `opening_loss.py` (оба читают общий `K_OPEN`, но не через общую функцию) — финальный ревьюер отдельно выделил как «стоит закрыть общим helper'ом при следующем касании файла», не блокирует. (4) `windowEnd`/`len(moves)` на фронтенде предполагает, что каждый нелистовой узел на пути — реальный ход (`B`/`W`), что при узле только с комментарием/сетапом разойдётся с бэкендовым расчётом и приведёт к громкому 422 (не тихой поломке) — тот же паттерн уже заложен в `App.tsx`'s `mainLineIds[msg.turnNumber]`, не новая дыра. (5) 422-тело FastAPI — массив объектов, `${body.detail}` в `explainOpening()`'s сообщении об ошибке рендерится как `[object Object]` — тот же паттерн, что уже есть у `explainPosition`, но `/api/explain/opening` первый эндпоинт, где 422 реалистичен как user-facing исход. (6) `_FINDING_FIELDS[finding.type]` в `consistency.py` — единственное оставшееся место, диспетчеризующее по типу находки без exhaustive-паттерна (`KeyError`→503 на гипотетический 4-й тип — громко, не тот класс тихого бага, что уже был дважды, но не помешало бы `.get()`+`AssertionError` для единообразия). (7) `moves[turn-1][0]` в `opening_loss.py` может дать `IndexError`/тихо смолчать на некорректный цвет — та же нестрогость, что уже есть у `ExplainRequest.moves`, не новая дыра. (8) `confidence=min(min_confidence_ratio, 1.0)` в `opening_loss.py` — избыточный `min()`, аккумулятор и так стартует с 1.0 и только уменьшается; безвредно. (9) Тест `buildOpeningSequence` на ветвление дерева проверяет путь, который в проде сейчас недостижим — `analysisByTurn` заполняется только по main line (`App.tsx`), так что поддержка вариаций в `buildOpeningSequence` пока «на вырост», а не используется. Отдельно: **несколько более старых записей выше в этом разделе («Где мы сейчас») помечены «готов к мержу (не смёржен)», хотя по факту уже смёржены в `main`** (индикатор разметки, Gemini/llama-cpp-провайдеры, детектор `mistake`, оба под-этапа Фазы 3 — RAG-цитаты в UI и подключение RAG к LLM; проверено через `git merge-base --is-ancestor` на все соответствующие коммиты) — текст этих записей не обновлялся после фактического мержа в более ранних сессиях. Не переписано в этой сессии как отдельная, не относящаяся к `opening_loss`, задача уборки документации — стоит поправить одним точечным проходом при следующем удобном случае.
- Фаза 3 и далее — не начинать без явного запроса пользователя (см. «Постоянные правила»).
- **Связь Фазы 2 ↔ Фазы 4 (миссия проекта — не потерять).** Миссия проекта: KataGo даёт объективный анализ, но ассистент должен учить через принципы рационального мышления и выявлять повторяющиеся ошибки/привычки, а не просто разбирать «лучший ход здесь» — чтобы игрок переносил понимание на новые, ранее не встречавшиеся позиции. Текущий дизайн это поддерживает (детекторы формализованы через структурные свойства позиции, а не память конкретных ходов; LLM по `§4` ARCHITECTURE.md интерпретирует находки через принципы/RAG, не переоценивает позицию; Player Passport — Фаза 4 — как раз хранит таксономию повторяющихся ошибок и тренд по времени). Но сейчас Фаза 2 (real-time объяснение партии) и Фаза 4 (накопленный паспорт) не связаны в дизайне — объяснение конкретной находки не подмешивает релевантные паспорт-тренды игрока («это твой N-й случай слабой группы через переизбыток за месяц»). Рассмотреть при проектировании Фазы 4 (или как ретроактивное расширение Фазы 2): передавать в LLM-промпт релевантные агрегаты паспорта для конкретного игрока, чтобы объяснения были habit-aware, а не только per-game.

## Decisions log

- 2026-08-03 — Выбор стека: Electron+TS frontend / Python backend, two-process sidecar-архитектура. → `docs/ARCHITECTURE.md`
- 2026-08-03 — UI-фреймворк фронтенда: Preact для всего фронтенда (не только доски). → `docs/ARCHITECTURE.md` § Frontend/UI
- 2026-08-03 — Plugin-контракт: framework-agnostic DOM/Custom Element, единый контракт для trusted/sandboxed режимов. → `docs/ARCHITECTURE.md` § Плагинная архитектура
- 2026-08-03 — Design-system стартер: Dark Mode (OLED) по умолчанию, Fira Code/Fira Sans, cool→hot ownership heatmap с numeric-фолбэком. → `docs/ARCHITECTURE.md` § UI/UX-принципы фронтенда
- 2026-08-03 — Введён постоянный workflow: git-ветки + `task_plan.md`/`findings.md`/`progress.md` для восстановления контекста между сессиями.
- 2026-08-04 — Frontend-стек Фазы 1 конкретизирован: SQLite/библиотека партий отложены до Фазы 4 (YAGNI); renderer делает HTTP/WS-запросы к backend напрямую (не через main-процесс); `uPlot` для winrate-графика; layout-вариант «доска доминирует, график во всю ширину снизу» (выбран визуально); `@sabaki/sgf`+`@sabaki/immutable-gametree` для SGF/дерева, `@sabaki/go-board` для позиции на доске (взятия автоматически), нативные пропы Shudan (`heatMap`/`lines`) для ownership/PV вместо кастомного оверлея. → `docs/superpowers/specs/2026-08-04-phase-1-frontend-design.md`
- 2026-08-04 — KataGo analysis config теперь явно задаёт `reportAnalysisWinratesAs = BLACK` (backend), иначе действует дефолт KataGo `SIDETOMOVE` — winrate/scoreLead меняли бы перспективу каждый ход, ломая frontend-график. Найдено и исправлено на финальном ревью frontend-ветки, подтверждено по исходникам KataGo. → `backend/src/baduk_backend/config/profile.py`
- 2026-08-05 — Ручная приёмка Фазы 1 (frontend+backend) с реальным KataGo пройдена и подтверждена пользователем после нескольких раундов правок на `fix-phase-1-acceptance-findings`. Ветка смёржена в `main` и запушена в `origin`.
- 2026-08-05 — Разобран весь отложенный backlog Фазы 1 на ветке `fix-phase-1-backlog-cleanup`: backend (WS `WebSocketDisconnect`, схемы с общим базовым классом и границами значений, `main.py` try/finally) + frontend (`mapSgfRules` warning, `GTP_COLUMNS` дедуп, `getConnection()` retry, экспорт `sgfError`). Попутно найдена и исправлена причина красного `pnpm run lint` — не только унаследованные scaffold-правила (как предполагалось ранее), а в основном CRLF из-за отсутствующего `.gitattributes` при `core.autocrlf=true`; добавлен `.gitattributes` (`* text=auto eol=lf`), дерево ренормализовано, `pnpm run lint` полностью зелёный. → `.gitattributes`, `backend/src/baduk_backend/api/`, `frontend/src/renderer/src/`
- 2026-08-05 — Живая отладка Electron-рендерера без интерактивных devtools: `mainWindow.webContents.on('console-message', ...)` пересылает консоль рендерера в stdout main-процесса (постоянно, дёшево); `BADUK_DEBUG_PORT` env var (опционально, через `app.commandLine.appendSwitch('remote-debugging-port', ...)`) открывает Chrome DevTools Protocol — оттуда можно снимать реальные скриншоты (`Page.captureScreenshot`) и выполнять JS в живом окне (`Runtime.evaluate`), включая синтетический `DragEvent('drop', {dataTransfer})` для прогона SGF без участия пользователя. Это и нашло реальные баги приёмки Фазы 1 (см. «Где мы сейчас»), а не гадание по описаниям. Осторожно: у Shudan класс `shudan-sign_1`/`shudan-sign_-1` стоит на 2 DOM-узлах на каждый камень (внешняя vertex-обёртка + внутренний stone-div) — считать камни через `querySelectorAll('.shudan-sign_1').length` даёт 2×. → `frontend/src/main/index.ts`
- 2026-08-05 — Фаза 2 явно попрошена пользователем, но сразу же сужена: полный объём Фазы 2 из `ARCHITECTURE.md` (несколько детекторов + calibration/backtesting harness + LLM Orchestrator с 3 провайдерами + двухступенчатая anti-hallucination) — это несколько связанных подсистем, а не один срез; по решению пользователя разбит на под-этапы, этот срез = только `weak_group` + только Claude + без калибровки + без свободных вопросов + полная anti-hallucination защита. Ownership/moveInfos для детектора берутся из уже полученного клиентом `AnalyzeResponse` (не новый вызов KataGo) — `/api/explain` намеренно не трогает `EngineManager`/`engine_lock`. Backend сам восстанавливает доску из `moves` (не доверяет frontend-вычисленному состоянию) — новый минимальный Go-движок (расстановка+взятия) в `backend/src/baduk_backend/board/`. → `docs/superpowers/specs/2026-08-05-phase-2-weak-group-explanation-design.md`
- 2026-08-05 — Пользователь поднял отдельную, не связанную с LLM возможность — ручная SGF-разметка/комментарии с сохранением в файл (буквы/цифры/фигуры). Явно отложена как отдельная будущая задача, начинается после этого среза Фазы 2, не раньше. → см. backlog.
- 2026-08-06 — Финальное whole-branch ревью (opus) Фазы 2/среза `weak_group` нашло реальный межзадачный баг, который не мог всплыть ни в одном task-level review: `async def`-хендлер `/api/explain` синхронно вызывал Claude API, замораживая весь event loop backend'а (включая `/api/analyze/stream`) на время до 3 последовательных вызовов — Task 5 (провайдер) написал синхронный вызов, Task 6 (эндпоинт) — `async def`, и только вместе они дали баг. Исправлено через `asyncio.to_thread(...)`, тем же паттерном, что уже используется в `/api/analyze`. Также найдено и исправлено: `Finding` не хранил цвет группы и не давал человекочитаемых GTP-координат — LLM получала сырые `(x,y)`-кортежи, которые игрок не может найти на доске; `max_tokens=1024` при adaptive thinking по умолчанию у Claude рисковал обрезать ответ и вместо спроектированного graceful fallback выдавать 503. → `backend/src/baduk_backend/api/explain.py`, `backend/src/baduk_backend/llm/providers/claude.py`, `backend/src/baduk_backend/feature_extraction/schemas.py`
- 2026-08-06 — Ручная SGF-разметка: пользователь явно попросил начать эту backlog-фичу. Дизайн: разметка/комментарии хранятся напрямую как SGF-свойства узла дерева (`tree.mutate()`), не в параллельном сторе — сохранение в файл тривиально (`sgf.stringify([tree.root])`). Пользовательская разметка на доске всегда в приоритете над PV-метками KataGo в той же точке. Путь исходного файла — через `webUtils.getPathForFile()` (уже доступен через существующий `@electron-toolkit/preload`, новых preload-изменений для этого не понадобилось). Редактирование дерева ходов и undo/redo — явно вне скоупа. → `docs/superpowers/specs/2026-08-06-manual-sgf-annotation-design.md`
- 2026-08-06 — При реализации ручной SGF-разметки трижды находился реальный баг в самом плане (не в реализации субагентов) — каждый раз подтверждён пользователем перед фиксом, т.к. исправление противоречило буквальному тексту уже утверждённого плана: (1) `sgf.stringify(tree.root)` без обёртки в массив даёт SGF без внешних скобок — невалидный, не откроется в стороннем ПО; (2) тулбар разметки в `BoardView.tsx` рендерился по плану даже без загруженной партии — позволяло «вооружить» инструмент разметки заранее, состояние переживало открытие файла; (3) `handleBlur` в `AnnotationPanel.tsx` коммитил изменение в дерево на каждый выход фокуса из поля комментария, даже без реальной правки текста. Итог сессии: буквальный, полностью расписанный implementation-план — не гарантия корректности; task-level review каждый раз ловил то, что не поймал бы beforehand-написанный код без независимой проверки. → `docs/superpowers/plans/2026-08-06-manual-sgf-annotation.md`
- 2026-08-06 — Финальное whole-branch ревью (opus) ветки `feature-sgf-annotation` нашло 6 находок уровня Critical/Important, ни одна не могла всплыть на task-level review: lint-долг из ошибок форматирования, накопленный за несколько задач, не проверялся построчно ни одним ревьюером до конца; `parseSgf` сбрасывает счётчик id на каждый разбор — root-узлы разных партий получают одинаковый numeric id, из-за чего `AnnotationPanel`'s `useEffect(..., [nodeId])` не перезагружался при смене файла (Task 6 писал эффект, Task 8 владел `loadGame` — баг виден только на пересечении); краш рендера доски на разметке с координатами вне текущего размера доски из стороннего SGF (нет bounds-check — актуально именно потому, что чтение чужой разметки — заявленная фича); неатомарная запись файла и риск молчаливой порчи партии при перезаписи в не-UTF8 кодировке (решение пользователя — предупреждать и отказывать перезапись, не пытаться полноценно поддерживать другие кодировки); выход через Cmd+Q убивал backend раньше, чем пользователь успевал отменить диалог о несохранённых изменениях (`before-quit` vs `will-quit` — Task 9 добавил обработчик `close`, но не тронул уже существующий из Фазы 1 `before-quit`). Все 17 находок (включая 8 Minor) исправлены одним consolidated fix-wave — по явному решению пользователя не разбивать на отдельные раунды. → `.superpowers/sdd/2026-08-06-manual-sgf-annotation/progress.md`
- 2026-08-06 — Пользователь сразу после приёмки SGF-разметки предложил мини-фичу «индикатор в дереве вариаций» (треугольник в кружке). Дизайн: один комбинированный индикатор (не различает комментарий/разметку), чистая функция `hasAnnotation(node)` от уже доступного `node.data` — никакого нового состояния/IPC/мутации. SVG-паттерн треугольника взят из `@sabaki/shudan`'s `Marker.js` для консистентности с уже нарисованными на доске треугольниками. → `docs/superpowers/specs/2026-08-06-variation-tree-annotation-indicator-design.md`
- 2026-08-06 — Финальное ревью ветки `fix-variation-tree-annotation-indicator` нашло 2 Important, оба — пробелы в тестовом покрытии (не архитектурные баги): тесты не были привязаны к конкретному узлу дерева через `aria-label`, поэтому баг «иконка на не том узле» прошёл бы незамеченным; не было теста на корневой узел, хотя дизайн-спек явно требует его поддержки. Один из 4 Minor-находок (смещение иконки на ~1px) прямо конфликтовал с пиксельными значениями, взятыми дословно из уже одобренного плана — по установленному в сессии правилу такие находки не фиксятся без решения пользователя; пользователь явно оставил это как есть вместе с a11y-находкой, выбрав в фикс-вейв только дедупликацию списка ключей разметки (`FIGURE_PROPERTIES` теперь экспортируется из `annotations.ts` вместо параллельного литерала в `VariationTree.tsx`). → `docs/superpowers/plans/2026-08-06-variation-tree-annotation-indicator.md`
- 2026-08-06 — Пользователь явно попросил добавить Gemini как LLM-провайдер. Уточняющий вопрос пользователя «зачем новый провайдер, а не единый унифицированный» разобран отдельно: рассмотрена и явно отклонена унификация через LiteLLM — `LLMProvider`-протокол уже унифицирует там, где это важно для остального кода (оркестратор/consistency-чекер), а унификация на уровне самого API-вызова добавила бы слой трансляции между кодом и реальным контрактом forced tool-use, что рискует главной anti-hallucination гарантией фичи; провайдеров ровно два, третий не планируется (YAGNI). Дефолт `BADUK_LLM_PROVIDER` — `"gemini"`, не `"claude"` (явное решение пользователя: основной сценарий сейчас — Gemini, ключа Claude нет). Модель по умолчанию `gemini-3.6-flash` выбрана через веб-поиск на момент дизайна (стабильна, бесплатный тариф; `gemini-2.5-flash` уходит под deprecation 16 октября 2026). → `docs/superpowers/specs/2026-08-06-gemini-llm-provider-design.md`
- 2026-08-06 — Финальное whole-branch ревью (opus) ветки `phase-2-gemini-provider` нашло Critical-баг, невидимый ни одному task-level review и ни одному unit-тесту на фейковом клиенте: `thinking_budget=0` (параметр линейки Gemini 2.5) был явно задан в плане для дефолтной модели `gemini-3.6-flash` (линейка Gemini 3, которой нужен `thinking_level`) — риск 400-ошибки или молчаливого игнорирования на каждом вызове дефолтного (после Task 3) провайдера. План сам мандатировал `thinking_budget=0` — правка отступала от буквального текста плана, поэтому решение подтверждено у пользователя перед фиксом (независимо перепроверено через веб-поиск и интроспекцию установленного `google-genai` 2.17.0: `ThinkingLevel` содержит `MINIMAL`/`LOW`/`MEDIUM`/`HIGH`). Ручная живая проверка (обязательная по плану, единственная, что поймала бы этот баг до мержа) с реальным пользовательским ключом дошла до Gemini API и корректно разобрала структурированный ответ — но получила `400 FAILED_PRECONDITION: User location is not supported for the API use` (гео-ограничение аккаунта/сети на стороне Google, не связано с кодом) вместо успеха. Пользователь явно одобрил мерж без подтверждённого полного round-trip — риск живой работы Gemini-провайдера в проде остаётся неснятым до тех пор, пока кто-то не прогонит его с ключом/сетью без этого ограничения. → `.superpowers/sdd/2026-08-06-gemini-llm-provider/progress.md`
- 2026-08-06 — Пользователь после мержа Gemini явно поднял проблему платных облачных API с лимитами, попросил спроектировать переход на `llama-cpp-python`. На уточняющий вопрос «зачем не унифицированный провайдер» пользователь сам ответил вопросом «какие локальные модели подойдут» — отдельный веб-поиск дал `Qwen3-8B` GGUF Q4_K_M под заявленное железо пользователя (NVIDIA ≤8 ГБ VRAM, та же карта, что уже гоняет KataGo). Дизайн: третий провайдер, а не замена облачных (подтверждено отдельным вопросом после ответа про модели); grammar-constrained JSON через `response_format` вместо forced tool-use — механически даже более строгая гарантия structured output; конфигурация модели через `BADUK_LLAMA_MODEL_PATH`/`BADUK_LLAMA_N_GPU_LAYERS` env var, файл модели не коммитится и не скачивается автоматически — тот же паттерн, что `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL`. → `docs/superpowers/specs/2026-08-06-llama-cpp-provider-design.md`
- 2026-08-06 — Перед написанием плана лично верифицирован точный API `llama-cpp-python` v0.3.34 по исходникам на GitHub (raw.githubusercontent.com), не по памяти — критично, т.к. библиотечные дефолты не подходят проекту без явного переопределения: `n_gpu_layers` дефолтится в `0` (не `-1`), `n_ctx` дефолтится в `512` (слишком мало для системного промпта + JSON-schema-грамматики + ответа), `response_format` — именно `{"type":"json_object","schema":...}` (ключ `"schema"`, не `"json_schema"`). Также лично воспроизведён Windows long-path build fail при `pip install llama-cpp-python` без готового wheel на этой машине — задокументирован в плане как известный обходной путь (готовый CUDA-wheel как основной маршрут установки, именно чтобы обойти сборку из исходников). → `docs/superpowers/plans/2026-08-06-llama-cpp-provider.md`
- 2026-08-06 — Финальное whole-branch ревью (opus) ветки `phase-2-llama-provider` нашло 3 Important: (1) `llama-cpp-python` был обязательной зависимостью в `pyproject.toml` — на машине без подходящего wheel (как эта самая машина) ломалась не только llama-функциональность, а сбор всего тест-сьюта целиком, включая Claude/Gemini/KataGo-тесты; исправлено переносом в `[project.optional-dependencies]` + `pytest.importorskip("llama_cpp")`; (2) единственный `Llama`-инстанс не был защищён локом при параллельных `/api/explain` (через `asyncio.to_thread`) — `llama_cpp.Llama` не потокобезопасен (подтверждено grep'ом по SDK — ни `threading`, ни `Lock`; собственный сервер библиотеки оборачивает вызовы в лок), риск порчи состояния/segfault всего backend-процесса; в проекте уже есть прецедент для другого stateful-ресурса — `engine_lock` для KataGo, здесь не применён; исправлено `threading.Lock` внутри `LlamaProvider`; (3) README не предупреждал, что модель грузится в VRAM до старта KataGo на той же ≤8-ГБ карте — реалистичный риск OOM, который бы проявился как загадочный сбой KataGo (стартует вторым), а не LLM. Ре-ревью фикс-вейва нашло residual gap в фиксе (1): `main.py`'s `"llama"`-ветка всё ещё импортировала `LlamaProvider` до проверки `BADUK_LLAMA_MODEL_PATH` (тот же порядок, что в claude/gemini-ветках плана, но там это безопасно — их SDK не имеют этой хрупкости) — подтверждено пользователем перед фиксом, т.к. это отступление от буквального текста плана; исправлено перестановкой порядка проверки/импорта, живо переверифицировано повторным запуском ранее падавшего теста в изоляции. → `.superpowers/sdd/2026-08-06-llama-cpp-provider/progress.md`
- 2026-08-06 — Живая проверка `llama-cpp-python`-провайдера с реальной моделью Qwen3-8B-Q4_K_M.gguf (путь предоставлен пользователем) прошла успешно — первый подтверждённый полный round-trip среди всех трёх LLM-провайдеров этой сессии (Claude ещё не проверялся вживую, Gemini упёрся в гео-ограничение). Инференс шёл на CPU (CUDA-сборка `llama-cpp-python` не загрузилась на этой машине — нет CUDA Toolkit runtime, только драйвер NVIDIA), заняло 123 секунды — GPU-оффлоад остаётся неподтверждённым, добавлено в backlog.
- 2026-08-06 — Пользователь явно решил: llama-cpp-python (локальный, бесплатный, без лимитов запросов) становится основным LLM-решением проекта — та же исходная мотивация, что привела к добавлению обоих облачных провайдеров (платные API с ограничениями). Дефолт `BADUK_LLM_PROVIDER` сменён с `"gemini"` на `"llama"` в `main.py::run()`. Claude/Gemini код и выбор через `BADUK_LLM_PROVIDER=claude`/`BADUK_LLM_PROVIDER=gemini` осознанно оставлены нетронутыми — не в фокусе тестирования/разработки, но полностью рабочие. → `docs/superpowers/specs/2026-08-06-llama-default-provider-design.md`
- 2026-08-11 — GPU-оффлоад `llama-cpp-python` подтверждён живьём через Vulkan-wheel (не CUDA — CUDA Toolkit по-прежнему не установлен на машине разработки). README дополнен Vulkan-инструкцией как рабочей альтернативой CUDA для машин без CUDA Toolkit. → `backend/README.md`
- 2026-08-11 — Пользователь явно попросил продолжить «остаток Фазы 2», выбрал начать с доп. детекторов (`mistake`/`opening_loss`) — не с калибровки/harness, не со свободных вопросов к LLM (те явно отложены как отдельные под-этапы того же брейнсторминга). В рамках дизайна `mistake` — два содержательных отклонения от буквального `ARCHITECTURE.md`: (1) `Δ` считается через разницу `rootInfo.scoreLead` двух полных позиций (до/после хода), а не через сопоставление `moveInfos`, как буквально написано в ARCHITECTURE.md — предпочтено явно, т.к. второй подход молчаливо не сработал бы именно на самых грубых ошибках (сыгранный ход мог не попасть в число исследованных KataGo кандидатов); (2) пороги `Δ` взяты не из иллюстративного примера ARCHITECTURE.md (`{opening:3, middlegame:2, endgame:1}`), а из реальной лестницы `eval_thresholds` открытого инструмента обучения кю-игроков KaTrain (`[12,6,3,1.5,0.5,0]` очков, без поправки на стадию — по решению пользователя), явно помечены как некалиброванные под этот проект. Отдельно проверен и явно отклонён как источник калибровки личный репозиторий пользователя `Baduk-knowledge-base` (152 карточки Go-принципов) — контент чисто качественный, без численных порогов; зафиксирован как прямое попадание в будущую Фазу 3 (RAG), не в калибровку детектора. → `docs/superpowers/specs/2026-08-11-phase-2-mistake-detector-design.md`
- 2026-08-12 — Живая проверка Фазы 3, первого под-этапа: реальный `BADUK_KNOWLEDGE_BASE_PATH` (личный репозиторий `Baduk-knowledge-base`, 452 карточки) + `test_ingest_and_retrieve_real_knowledge_base` — PASSED за 27 минут (первая загрузка BGE-M3 ~2.3 ГБ + CPU-эмбеддинг всей базы). `retrieve_knowledge()` вернул `two-eyes-necessary-for-unconditional-life` в top-3 для известного запроса — критерий готовности спека подтверждён не только на фейковой embedding-модели юнит-тестов, но и вживую. `backend/rag_store/` (~16 МБ) подтверждено гитигнорится. Ветка `phase-3-rag-ingestion` смёржена в `main` (fast-forward `70f7715..71171db`) по выбору пользователя перед этой проверкой.
- 2026-08-11 — Финальное whole-branch ревью (opus) ветки `phase-3-rag-ingestion` нашло 6 Important, из которых 4 ревьюер прямо квалифицировал как дефекты самого плана, а не отступления имплементеров: (а) Global Constraint плана («no changes to any existing file outside `backend/pyproject.toml`») буквально противоречил спеку, требующему гитигнорить `rag_store/` — план механически запретил ровно те правки (`.gitignore`, README), без которых срез не production-ready; (б) требование спека «BGE-M3 локально на CPU» потерялось при переходе спек→план — эскиз `store.py` в плане не передавал `device="cpu"`, требование выполнялось только случайно (CPU-only torch на этой машине); (в) план сам содержал вырожденный ассерт `len(snippets) <= 2`, который проходит и на пустом списке; (г) план декларировал правило `pytest.importorskip(...)` в Global Constraints, но собственный код Task 1 (`test_cards.py`) его не применял, из-за чего чистая установка без `[rag]`-экстры роняла 4 теста вместо skip. Оставшиеся 2 Important — реальные implementation-гэпы: опечатка в `BADUK_KNOWLEDGE_BASE_PATH` молча уничтожала существующий индекс (`Path.glob()` на несуществующей директории возвращает `[]`, не бросает), README не документировал новую env var. Все 6 Important + 4 отобранных Minor резолвлены контроллером напрямую в одном фикс-вейве без обращения к пользователю — ни один не затрагивал реальные дизайн-решения с брейнсторминга (BGE-M3/Chroma/full-rebuild/`RagSnippet`), только приводил реализацию к уже одобренному спеку. → `.superpowers/sdd/2026-08-11-phase-3-rag-ingestion/progress.md`
- 2026-08-12 — Пользователь явно попросил показать RAG-цитаты в UI (третий под-этап Фазы 3). Брейнсторминг выявил, что это требует не только frontend-работы — backend отдавал только голый `rag_doc_id`, недостаточный для карточки цитаты. Решение: `get_snippet_by_id` — точечный lookup по id (`collection.get(ids=[doc_id])`, не семантический поиск, не принимает `embedding_model`) + новое поле `ExplainResponse.citation`; frontend — нативный `<details>/<summary>`, секция не рендерится без цитаты. Финальное whole-branch ревью (opus) нашло 3 Important: незащищённый блок обогащения в `explain.py` мог уронить готовый ответ в 500 при непредугаданной ошибке Chroma (`chromadb.errors.InternalError` — не `RuntimeError`, ничем не ловился; воспроизведено живьём); у развёрнутой карточки цитаты не было ограничения по высоте — на реальной карточке (3122 символа/64 строки) это схлопывало ряд с доской в ноль (замерено на реальных данных, не предположено); `typecheck:web` не покрывает `frontend/tests/**` — верификационный шаг плана («без `citation: null` в моках typecheck упадёт») был холостым — это отдельный, project-wide tooling-гэп, не дефект этой ветки, отложен в backlog, а не исправлен молча в фикс-вейве. Оба первых Important закрыты фикс-вейвом, re-review подтвердил ADDRESSED. → `.superpowers/sdd/2026-08-12-phase-3-rag-citations-ui/progress.md`
- 2026-08-12 — Пользователь явно попросил подключить первый под-этап Фазы 3 (retrieve_knowledge, уже в main) к LLM-пайплайну. Брейнсторминг сузил объём против исходного видения ARCHITECTURE.md: только backend (без UI-цитат), только llama-cpp-python (Claude/Gemini не трогаются — у них уже есть родной forced tool-use, но это отдельная будущая задача), гибридная agentic-схема (модель решает искать/не искать, но текст query строим мы детерминированно из полей Finding — не полная агентность), `oneOf`-JSON-схема с ровно одним хопом поиска на вызов (не многошаговый агентный цикл), `rag_doc_id`-цитата с anti-hallucination проверкой сразу в этом срезе (не отложена). Технический риск главной идеи — поддержка `oneOf`/`const` в JSON-schema→grammar конвертере `llama.cpp`/`llama-cpp-python` — лично проверен по исходникам (`json_schema_to_grammar.py`/`llama_grammar.py`) перед дизайном, не предположен. → `docs/superpowers/specs/2026-08-12-phase-3-rag-llm-wiring-design.md`
- 2026-08-12 — Финальное whole-branch ревью (opus) ветки `phase-3-rag-llm-wiring` нашло 4 Important, 3 из которых — дефекты спека, не имплементации: (а) спек оценил стоимость дублирующего вызова `retrieve_knowledge` (реальный поиск в `llama.py` + независимый пересчёт в `consistency.py`) как «миллисекунды» — реальный замер на этой машине показал 7.5–14.4с на незакэшированную конструкцию `SentenceTransformer("BAAI/bge-m3")`, то есть без кэша один `/api/explain`-запрос мог грузить модель до 8 раз (~60с) поверх самого llama.cpp-инференса; (б) `top_k=3` для `retrieve_knowledge` был продублирован как независимый литерал и в `llama.py`, и в `consistency.py`, хотя `build_rag_query` уже был явно задуман общим контрактом между ними — расхождение тихо ломало бы anti-hallucination проверку при будущей настройке одного без другого; (в) ни один промпт не инструктировал модель фактически заполнять опциональное поле `rag_doc_id` — вся 119-строчная anti-hallucination механика цитат в `consistency.py` редко бы срабатывала не потому что модель галлюцинирует, а потому что её никто не просил цитировать вообще. Четвёртый Important — implementation-level: `_rag_doc_id_valid()` вызывался дважды за одну итерацию retry-цикла (из `_is_verified` и из `_build_corrections` независимо), удваивая теперь-уже-нетривиальную стоимость операции. Один консолидированный фикс-вейв (кэш эмбеддинг-модели через `functools.lru_cache`, `RAG_TOP_K` перенесён в общий `prompts.py`, инструкция по `rag_doc_id` добавлена в `_format_snippets()`, дедуп вызова `_rag_doc_id_valid`, плюс 2 отобранных Minor) + scoped re-review (все 6 находок ADDRESSED, включая живой замер cache-identity: 12.2с холодный вызов → 0.0с и тот же объект на повторном) закрыли всё. → `.superpowers/sdd/2026-08-12-phase-3-rag-llm-wiring/progress.md`
- 2026-08-11 — Пользователь явно попросил начать Фазу 3 (RAG). Контент — готовый личный репозиторий `Baduk-knowledge-base` (452 карточки), не сбор с нуля; все три типа карточек индексируются как независимые плоские чанки (одна карточка = один чанк, без деления на секции); реальные категории базы заменяют иллюстративные из ARCHITECTURE.md; эмбеддинги — BGE-M3 локально (не API — тот же local-first принцип, что и решение сделать `llama-cpp-python` дефолтным LLM-провайдером); векторное хранилище — Chroma; индексация — полная пересборка по запросу (`python -m baduk_backend.rag.ingest`), не инкрементальная синхронизация; срез явно ограничен ingestion+retrieval, без подключения к LLM-пайплайну/frontend (следующий под-этап). → `docs/superpowers/specs/2026-08-11-phase-3-rag-ingestion-design.md`
- 2026-08-15 — Пользователь явно попросил продолжить «остаток Фазы 2», выбрал начать с `opening_loss` (не с калибровки/harness, не со свободных вопросов к LLM — оба остаются отдельными под-этапами). Ключевые решения брейнсторминга: Δ на каждом ходе — та же формула, что у `mistake`, вынесенная в общий `scoring.py`; окно дебюта переиспользует существующую `K_OPEN` (единое определение «дебюта» на весь проект, не отдельный N); фронтенд собирает компактную дебютную последовательность из уже закэшированного `analysisByTurn` (не новые запросы к KataGo); накопленная потеря — два разных числа (отдельно чёрные/белые), пользователь выбирает цвет явно новой кнопкой «Проанализировать дебют»; отдельный эндпоинт `POST /api/explain/opening` (не расширение `/api/explain` — другая форма входа); backend+frontend в одном срезе (не отложено, в отличие от RAG-LLM-wiring). Пороги (`THRESHOLD_OPENING_LOSS=3.0`, `SEVERITY_MEDIUM=5.0`, `SEVERITY_HIGH=15.0`) — иллюстративные, без внешнего ориентира (в отличие от лестницы KaTrain у `mistake`); само-ревью спека поймало и поправило числовую нестыковку (`threshold` изначально совпадал с `SEVERITY_MEDIUM`, что сделало бы `severity="low"` недостижимым). → `docs/superpowers/specs/2026-08-15-phase-2-opening-loss-detector-design.md`
- 2026-08-15 — Финальное whole-branch ревью (opus) ветки `phase-2-opening-loss-detector` нашло 3 Important, все — дефекты плана (буквальный код-скетч Task 6 скопирован имплементером один в один), не имплементации. Самое серьёзное: `analysisAtEnd` в запросе `/api/explain/opening` брался из анализа **текущего узла**, а не границы дебютного окна, как того требовал сам спек — при навигации далеко за пределы дебюта LLM получал `rootInfo` совсем другой позиции, а anti-hallucination checker (`consistency.py`) мог по ошибке подтвердить как «верные» числа, процитированные из этой чужой позиции (прямой удар по anti-hallucination гарантии, вокруг которой построен весь LLM-пайплайн проекта). Фикс-вейв закрыл все 3 находки, но re-review сам фикс сброса «при загрузке другой партии» (по ссылочному равенству `tree`) нашёл, что этот фикс внёс новую регрессию: `currentTree.value` пересоздаётся не только при загрузке файла, но и при любой разметке/правке комментария на той же партии (`AnnotationPanel.tsx`/`BoardView.tsx` вызывают `tree.mutate()`) — независимо подтверждено контроллером через grep обоих файлов. По правилам скила `subagent-driven-development` («no second fix wave» для финального ревью) находка не пошла в очередной автоматический раунд — контроллер вынес её пользователю явно, со списком всех принятых по ходу решений («Rulings I made»), не решив вопрос самостоятельно. Пользователь выбрал исправить сейчас; отдельный явный (не автоматический) фикс-раунд ввёл новый сигнал `gameLoadSequence` (инкрементируется ровно один раз в `App.tsx`'s `loadGame()`, не трогается мутациями разметки/комментариев) вместо ссылки на `tree`. Анти-регрессионное требование самого плана (exhaustive `match` вместо двухветочного `if/else` — класс бага, что уже дважды всплывал в истории проекта, второй раз — Critical на `phase-2-mistake-detector`, см. запись ниже) независимо перепроверено финальным ревьюером по всему репозиторию через grep всех мест диспетчеризации по `finding.type` — чисто, ничего не пропущено. → `.superpowers/sdd/2026-08-15-phase-2-opening-loss-detector/progress.md`
- 2026-08-11 — Финальное whole-branch ревью (opus) ветки `phase-2-mistake-detector` нашло Critical-регрессию на уже существующем (не новом) пути `weak_group`: `EXPLANATION_TOOL_PARAMETERS.cited_field` — единый enum на оба типа находок, но `_FINDING_FIELDS` в `consistency.py` стал per-type; модель могла процитировать схема-легальное, но «чужое» для текущего типа находки поле (напр. `delta_score` для `weak_group`-находки) — `_true_value()` проваливался в `getattr(analysis.rootInfo, field)`, `RootInfo` не имело такого атрибута, `AttributeError` не ловился нигде до `explain.py`'s блока `except Exception` → **HTTP 503** вместо спроектированного корректирующего повтора. Ни один из 5 task-level review этого не поймал, т.к. каждый тестировал только корректно типизированные claims. Независимо перепроверено ре-ревьюером через собственный repro-скрипт после фикса (`_true_value` теперь возвращает `None` вместо падения, `_claim_matches`/`_correction_message` трактуют `None` как несовпадение с понятным сообщением модели). Также в этом же ревью — обнаружен и исправлен настоящий off-by-one в `MistakeFinding.turn_number` (эндпоинт передавал позицию хода вместо порядкового номера сыгранного хода) и смешение двух систем отсчёta (`scoreLead`/`winrate` — всегда чёрные, `delta_score` — с точки зрения игрока) без пояснения в промпте — численно верная, но по стороне неверно атрибутированная claim проходила бы verification. → `.superpowers/sdd/2026-08-11-phase-2-mistake-detector/progress.md`
