# Progress Log — лог для восстановления контекста

Каждая сессия добавляет запись сюда **перед завершением работы**: что сделано, что решено, что осталось открытым, с чего начинать дальше. Новая сессия читает этот файл (плюс `task_plan.md` и `findings.md`) вместо того, чтобы расспрашивать пользователя заново.

## 2026-08-03 — Архитектурная сессия

**Сделано:**
- Проведён дизайн-ревью фронтенда через скилл `ui-ux-pro-max` (поначалу без Python — заглушка Windows Store; после установки Python 3.12.10 пользователем перезапущен с реальным поиском по базе). Результаты — в `findings.md`.
- Добавлен раздел «UI/UX-принципы фронтенда» в `docs/ARCHITECTURE.md`.
- Закрыт открытый вопрос по UI-фреймворку фронтенда (был отмечен как нерешённый в предыдущей правке): выбран **Preact** для всего фронтенда + **framework-agnostic DOM/Custom Element** контракт для плагинных точек расширения. Решение принято через сравнение (Preact/React/Svelte/Vanilla+WC) с участием Plan-агента, подтверждено пользователем через явный выбор.
- Внесены точечные правки в `docs/ARCHITECTURE.md`: § Frontend/UI (обоснование Preact), § Плагинная архитектура (конкретный контракт `registerAnalysisPanel`/`registerBoardOverlay`/`registerSettingsSection`, единство trusted/sandboxed режимов, проброс design-tokens в iframe), § UI/UX-принципы фронтенда (снята пометка «открытый вопрос»).
- По запросу пользователя настроен постоянный workflow на будущее: git-ветки для разработки (не коммитить в `main` напрямую) + три файла планирования (`task_plan.md`, `findings.md`, `progress.md` — этот файл) для восстановления контекста между сессиями.

**Не сделано / решено не делать:**
- Код и структура проекта не создавались — репозиторий остаётся на стадии «архитектура утверждена, реализация не начата» согласно `CLAUDE.md`; это правило не нарушалось.
- Персистентная дизайн-система через `--persist` скила `ui-ux-pro-max` (`design-system/baduk-assistant/MASTER.md`) — пользователь выбрал вместо этого просто раздел в `ARCHITECTURE.md`; если понадобится персистентная версия для конкретных страниц UI в Фазе 1, это отдельная будущая задача.

**Открытые вопросы:** нет на данный момент (см. `task_plan.md` → Decisions log).

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Текущие задачи» и «Будущие задачи».
2. Если пользователь просит начать Фазу 1 — сначала создать git-ветку `phase-1-viewer-katago`, отметить это в `task_plan.md`, и только потом приступать к коду.

## 2026-08-03 — Реализация backend-части Фазы 1

**Сделано:**
- Создана ветка `phase-1-viewer-katago`; закоммичены ранее висевшие в рабочей директории `CLAUDE.md`/`docs/`/файлы планирования в `main`.
- Написан дизайн-спек `docs/superpowers/specs/2026-08-03-phase-1-viewer-katago-design.md` (brainstorming): Shudan, pnpm, uv, Vitest/pytest, electron-vite, vertical-slice подход, пути к локальному KataGo.
- Написан и выполнен через `subagent-driven-development` план `docs/superpowers/plans/2026-08-03-phase-1-backend-engine-manager.md` (5 задач, каждая — отдельный имплементер-субагент + ревью, при находках — раунд фикса + ре-ревью):
  1. Backend-скелет (FastAPI health-check + токен-аутентификация, sidecar-старт).
  2. `KataGoProfile` + рендеринг `.cfg`-конфига.
  3. `EngineManager` (жизненный цикл процесса, JSON stdin/stdout через reader-поток+очередь) — по ревью добавлен дренаж stderr (иначе риск deadlock на реальном KataGo).
  4. Тесты падения процесса/авто-restart.
  5. Реальный integration-тест против локального KataGo пользователя.
- В Task 5 по ходу работы: (а) пользователь попросил заменить файл `local_config.json` на переменные окружения `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL` — применено; (б) первый прогон завис на 80+ минут из-за повторного OpenCL-тюнинга с нуля — найдена причина (KataGo по умолчанию не переиспользует существующий тюнинг-кэш KaTrain), добавлен параметр `home_data_dir_override` в `render_analysis_config`, зависший процесс убит, повторный прогон прошёл за 5.27с с реальными winrate/ownership/PV.
- Финальное ревью всей ветки (модель opus) нашло 7 находок уровня Important — все исправлены одним fix-wave (редактирование реальных путей из двух doc-файлов, приведение плана в соответствие с env-var дизайном, `.gitignore`-подстраховка, `encoding="utf-8"` в `Popen`, закрытие pipe-хендлов в `stop()`, 401 вместо 422 для отсутствующего токена + `secrets.compare_digest`, заполнение `backend/README.md`). Ре-ревью фикса — чисто.
- Обновлены `task_plan.md`/`progress.md` (это правило самого проекта, ранее не соблюдалось по ходу выполнения плана — отмечено финальным ревью).

**Не сделано / решено не делать (осознанно отложено):**
- Пересинхронизация запрос/ответ в `EngineManager` после `TimeoutError` и блокировка от конкурентных `analyze()` — отложены на будущий API-слой (не актуальны, пока вызовы последовательные).
- Frontend (Electron/Preact, Shudan-доска, IPC-клиент, overlay-панели) — вообще не начат, отдельный будущий план(ы) согласно дизайн-спеку (шаги 1/4/5/6 vertical slice).
- Пара мелких находок оставлена как есть (см. `.superpowers/sdd/.../progress.md` этого плана): гипотетическая гонка при закрытии pipe в `stop()` (не воспроизведена), две устаревшие фразы про `local_config.json` в дизайн-спеке (не реальные пути, просто текст).
- Ветка `phase-1-viewer-katago` **не смёржена** в `main` — следующий шаг сессии.

**Открытые вопросы:** нет по существу; есть организационный вопрос — смёржить ветку сейчас или продолжать в ней же frontend-часть (см. `task_plan.md` → «Текущие задачи»).

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Текущие задачи».
2. Решить судьбу ветки `phase-1-viewer-katago` через `superpowers:finishing-a-development-branch`, если это ещё не сделано.
3. Если продолжаем Фазу 1 — писать план(ы) для frontend-части (board+SGF, затем IPC+overlay-панели+сквозная приёмка), см. дизайн-спек.

## 2026-08-04 — API-слой над EngineManager (HTTP+WS)

**Сделано:**
- Brainstorming недостающего API-слоя (обнаружен при планировании frontend-части: backend отдавал только `/health`, шаг 3 vertical slice — HTTP/WS роуты над `EngineManager` — не был реализован) → дизайн-спек `docs/superpowers/specs/2026-08-03-phase-1-backend-api-design.md`, ветка `phase-1-backend-api`.
- Детальный implementation-план `docs/superpowers/plans/2026-08-03-phase-1-backend-api.md` (5 задач, полностью самодостаточных — весь код прописан заранее).
- Выполнено через `subagent-driven-development`: Task 1 (Pydantic-схемы), Task 2 (drain-фикс стейл-очереди в `EngineManager.analyze()`), Task 3 (`auth.py` + `POST /api/analyze` + wiring `app.state.engine_manager`/`engine_lock` в `main.run()`), Task 4 (`WS /api/analyze/stream` с прогрессом по ходам), Task 5 (реальный integration-тест через HTTP-слой, подтверждён на локальном KataGo). Каждая задача — отдельный ревью, все прошли чисто (только Minor-находки, запаркованы в лог).
- Финальное whole-branch ревью (opus) нашло 5 Important-находок, эмпирически подтверждённых (не только по чтению диффа): (1) ответ-ошибка KataGo (`{"error": ...}`) валился в 500/обрыв WS вместо типизированной ошибки; (2) `TimeoutError`/`ValueError` из `EngineManager.analyze()` не ловились ни в одном эндпоинте; (3) не-ASCII токен ронял `secrets.compare_digest` в `TypeError`→500 на неаутентифицированном пути; (4) `EngineManager` никогда не останавливался в `main.run()` — процесс `katago.exe` оставался сиротой на Windows при выходе родителя, плюс утечка temp `.cfg`; (5) не было теста, подтверждающего, что `asyncio.Lock` реально сериализует конкурентные запросы (ревьюер экспериментально показал: без лока 2 из 3 параллельных запросов падают).
- Всё исправлено одним fix-wave (3 коммита) + один scoped re-review — все 5 ADDRESSED, один Minor запаркован (не воспроизводит исходную проблему).
- По ходу ревью подтверждено: запрет на хардкод путей KataGo (переменные окружения) не нарушен нигде в ветке.
- Ветка `phase-1-backend-api` смёржена в `main` локально (fast-forward, e748244..f83b307) через `superpowers:finishing-a-development-branch`; полный набор тестов (33 unit/route) перепроверен зелёным на слитом результате; ветка удалена.

**Не сделано / решено не делать (осознанно отложено):**
- Дублирование полей `AnalyzeRequest`/`StreamAnalyzeRequest` (нет общего базового класса) — оставлено, т.к. асимметрия ограничений (`analyzeTurns` ровно 1 vs `turnNumbers` ≥1) содержательна.
- WS не ловит `WebSocketDisconnect` при отключении клиента посреди стрима — не критично (движок не жжёт время впустую), просто необработанное исключение в логах.
- `turnNumbers` не ограничен сверху по длине; схемы без границ значений (`maxVisits`/`komi`/`boardXSize`/`boardYSize`) — YAGNI для однопользовательского десктоп-сценария сейчас.
- `main.py`'s `try/finally` не покрывает `_find_free_port()`/`print()` перед входом в `try` — крайне маловероятный failure-window, не воспроизводит утечку процесса.
- Frontend (Electron/Preact, Shudan-доска, IPC-клиент, overlay-панели) — по-прежнему не начат; ветка `phase-1-frontend` ждёт продолжения (нужен rebase на текущий `main`, поскольку API-слой теперь там).

**Открытые вопросы:** нет.

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Текущие задачи».
2. Переключиться/rebase на `phase-1-frontend`, продолжить: board+SGF, затем IPC-клиент+overlay-панели+сквозная приёмка (backend теперь полностью готов и в `main` — есть на чём тестировать реальный IPC).

## 2026-08-04/05 — Frontend-часть Фазы 1: brainstorming + implementation + ревью

**Сделано:**
- Ветка `phase-1-frontend` перебазирована на актуальный `main` (единственный старый коммит — устаревшая правка `task_plan.md`, полностью перекрыта, rebase дал пустой коммит).
- По ходу — отдельный вопрос от пользователя о соответствии архитектуры заявленной миссии проекта (объективный анализ KataGo + обучение через принципы рационального мышления, а не разбор отдельных ходов); подтверждено, что текущий дизайн это поддерживает, найдена и записана в backlog связь Фаза 2↔Фаза 4 (habit-aware объяснения), не потеряна.
- Brainstorming frontend-части (продолжение прерванного в прошлой сессии) — теперь с конкретными backend-интерфейсами вместо гипотетических. Впервые в этом проекте использован визуальный companion (браузерная вкладка) для сравнения 3 вариантов layout — пользователь выбрал визуально. Дизайн-спек `docs/superpowers/specs/2026-08-04-phase-1-frontend-design.md`.
- Implementation-план `docs/superpowers/plans/2026-08-04-phase-1-frontend.md` — 11 задач. Перед написанием кода API незнакомых библиотек (`@sabaki/shudan`, `@sabaki/sgf`, `@sabaki/immutable-gametree`, `@sabaki/go-board`, `uPlot`, `electron-vite`) были верифицированы отдельными research-агентами против первоисточников — самопроверка плана поймала и исправила 3 реальных бага в черновике (неверная формула heatmap-strength, дублирование функции `pvToLines`, нерабочая кнопка "Повторить").
- Выполнено через `subagent-driven-development`: 11 задач (scaffold → backend-connection → SGF/дерево → IPC-клиент → GTP-request-builder → board-position(`@sabaki/go-board`) → app-state(`@preact/signals`) → доска(Shudan) → дерево вариаций+клавиши → overlay-панели(heatmap/PV/uPlot) → AppShell-интеграция). 6 задач прошли один fix-round каждая (мелкие находки — тип-safety, обработка ошибок, orphaned-процессы на Windows). Один имплементер-субагент был прерван лимитом сессии Anthropic посреди Task 8 — возобновлён на том же агенте без потери контекста, ничего не сломалось (изменений ещё не было к моменту обрыва).
- Финальное whole-branch ревью (opus) нашло 7 Important-находок, все реальные интеграционные баги, которые пер-таск ревью не могли увидеть (каждая задача по отдельности была корректна): rectangular-board SGF ронял рендер вместо явной ошибки; второй загруженный SGF не закрывал предыдущий WS-стрим (смешивание анализа двух партий); узлы вариаций вне основной линии молча показывали чужой (main-line) анализ; winrate-график вероятно рисовал KataGo's SIDETOMOVE-перспективу под неизменной подписью "Winrate (B)" (подтверждено по исходникам KataGo — дефолт `reportAnalysisWinratesAs` действительно `SIDETOMOVE`); ownership-heatmap показывал числовые подписи на всех 361 точках вместо только по hover (пробел самого плана — Task 10 не повторил hover-требование из findings.md); `streamAnalysis` не ловит WS close/error (UI зависает в 'streaming' навсегда при обрыве); документация проекта (`CLAUDE.md`/`progress.md`/`task_plan.md`) не обновлена под эту ветку.
- 5 из 7 находок исправлены одним fix-wave (включая правку `backend/src/baduk_backend/config/profile.py` — единственное изменение backend в этой сессии, найденное только благодаря тому, что frontend впервые дал возможность увидеть график). Ре-ревью фикса — чисто (один новый Minor, запаркован).
- Находка про `streamAnalysis` без close/error — адъюдицирована контроллером как не load-bearing (второй fix-wave правилами skill'а не предусмотрен), запаркована с рекомендацией почини́ть первой при возврате к frontend-коду.
- `CLAUDE.md`/`task_plan.md`/этот файл обновлены напрямую контроллером (не через субагента) — статус проекта, реальные команды сборки/тестов/линта, backlog минорных находок.

**Не сделано / решено не делать (осознанно отложено):**
- `streamAnalysis` WS close/error handling — см. выше, приоритетно перед Фазой 2.
- Ручная сквозная приёмка Фазы 1 целиком с реальным backend+KataGo (drag&drop → дерево+доска → live winrate/ownership) — ни разу не выполнена за всю сессию; единственная ручная проверка была для error-состояния `ConnectionGate` без backend (скриншот подтверждён). Приоритетно перед Фазой 2.
- `pnpm run lint` красный (унаследованные от scaffold'а правила, но нарушены новым кодом) — запарковано в backlog, не блокирует функциональность.
- Ряд более мелких находок (CSS-классы без стилей, дублирование `GTP_COLUMNS`, `mapSgfRules` без предупреждения, `getConnection()` кэширует rejected promise, `sgfError` не экспортирован для тестов) — см. `task_plan.md` → backlog, полный список.
- Ветка `phase-1-frontend` **не смёржена** — следующий шаг сессии.

**Открытые вопросы:** нет по существу; организационный вопрос — как интегрировать ветку (см. `task_plan.md`).

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Текущие задачи» и backlog (два приоритетных пункта перед Фазой 2: WS close/error handling, ручная приёмка с реальным KataGo).
2. Решить судьбу ветки `phase-1-frontend` через `superpowers:finishing-a-development-branch`, если это ещё не сделано.

## 2026-08-05 — Мёрж frontend-ветки, ручная приёмка Фазы 1 с реальным KataGo, фиксы

**Сделано:**
- Ветка `phase-1-frontend` смёржена в `main` (fast-forward, `superpowers:finishing-a-development-branch`), удалена вместе с SDD-воркспейсом.
- Обнаружен и исправлен пробел: в репозитории не было корневого `.gitignore`, из-за чего scratch-директория `.superpowers/` (brainstorming/SDD tooling) постоянно попадала в untracked. Добавлен `.gitignore`.
- **Ручная сквозная приёмка Фазы 1 с реальным KataGo** (`.katrain`, opencl-сборка v1.16.0) — впервые за всё время проекта. Сначала — прямой скриптовый прогон `POST /api/analyze` и `WS /api/analyze/stream` против реального backend+KataGo (без Electron) для проверки чистого контракта — прошёл, winrate/scoreLead стабильно с одной перспективы (подтверждает прошлый фикс `reportAnalysisWinratesAs=BLACK`). Затем — реальный Electron-запуск с drag&drop тестовой SGF.
- Пользователь нашёл через несколько раундов реального тестирования: дерево вариаций — не дерево (сначала текст вместо точек, потом диагональ вместо колонки, потом горизонталь, потом чёрные фишки сливались с тёмным фоном), стрелки/скролл навигации требовали фокуса и не работали как ожидалось (добавлена глобальная навигация + колесо мыши), доска не масштабировалась и не показывала маркер последнего хода, PV рисовался непонятными линиями (заменено на пронумерованные маркеры кандидатов), WS-ошибка и полностью пустая панель анализа (winrate/score/heatmap/PV), позже — не синхронизированная с ходом легенда графика и нечитаемые (сливающиеся с фоном) подписи осей.
- Корневая причина самого серьёзного бага (WS-ошибка + пустая панель анализа) — `frontend/src/renderer/index.html`'s CSP: `default-src 'self'` без `connect-src` блокировал вообще любой `fetch`/`WebSocket` рендерера к backend (порт backend'а динамический — никогда не тот же origin, что `'self'`). IPC работал (не проходит через CSP), поэтому ConnectionGate ошибочно показывал "ready".
- Второй серьёзный баг — неограниченный `ResizeObserver`-цикл в `BoardView` (observer наблюдал за контейнером, который сам менялся из-за перерисовки `BoundedGoban` внутри), тысячи `"loop completed with undelivered notifications"` в секунду, скорее всего маскировавшие обновления UI под собственным шумом. Пофикшено округлением + пропуском no-op обновлений.
- Для отладки без интерактивных devtools добавлены на постоянной основе: пересылка консоли рендерера в stdout main-процесса, и опциональный `BADUK_DEBUG_PORT` (Chrome DevTools Protocol). Через CDP — реальные скриншоты (`Page.captureScreenshot`) и доверенный ввод (`Input.dispatchKeyEvent`/`dispatchMouseEvent`, не JS-синтетика) — самостоятельно прогнал полный сценарий (drag&drop через синтетический `DragEvent`, навигация, наведение на график) и нашёл ещё баги до возврата к пользователю: uPlot трактовал номера ходов как unix-время ("1/1/70"), `setCursor()` двигал только крестик графика, но не легенду (нужен отдельный `setLegend()`), подписи осей рисуются на canvas и не подчиняются CSS — дефолтный тёмно-серый цвет сливался с тёмной темой.
- Ложная тревога, найденная и опровергнутая через CDP: показалось, что один ArrowDown продвигает на 2 хода — оказалось, у Shudan класс `shudan-sign_1`/`shudan-sign_-1` стоит на 2 DOM-узлах на каждый камень (внешняя vertex-обёртка + внутренний stone-div), из-за чего подсчёт через `querySelectorAll('.shudan-sign_1').length` давал 2×. Реальная навигация всегда была верной.
- Все правки — на ветке `fix-phase-1-acceptance-findings` (16 коммитов, каждый фикс отдельно), тесты/typecheck зелёные на каждом шаге (frontend 46 тестов, backend 33). Пользователь подтвердил финальный раунд ("Все ок"). Ветка смёржена в `main` (fast-forward) и запушена в `origin`.
- По ходу пользователь дважды переспрашивал про количество незакоммиченных файлов — оба раза не было реальной проблемы (нормальный WIP на ветке, потом — ожидаемая разница при `git checkout main`), но подсветило, что стоит коммитить чаще и явнее объяснять промежуточное состояние git.
- Один раз я преждевременно написал "приёмка пройдена", хотя проверил только сам (CDP), не пользователь — пользователь справедливо поправил; статус в `task_plan.md` был скорректирован до собственноручного подтверждения.

**Не сделано / решено не делать (осознанно отложено):**
- Ownership-heatmap на партии с малым числом визитов выглядит визуально «залитой» почти полностью одним цветом — осознанно не трогали, неясно баг это рендера или честное отражение шумной низковизитной оценки KataGo. См. `task_plan.md` backlog.
- Остальные минорные находки (lint, `mapSgfRules`, `GTP_COLUMNS` дублирование и т.д.) — см. `task_plan.md` backlog, без изменений.

**Открытые вопросы:** нет.

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Где мы сейчас»/backlog.
2. Фаза 1 полностью готова и в `main`+`origin`. Фаза 2 (LLM-объяснения) — не начинать без явного запроса пользователя.

## 2026-08-05 (продолжение) — Разбор backlog Фазы 1

**Сделано:**
- Ветка `fix-phase-1-backlog-cleanup` (от `main`). Пользователь выбрал этот шаг явно (через AskUserQuestion) вместо начала Фазы 2.
- Backend: WS-хендлер `/api/analyze/stream` теперь ловит `WebSocketDisconnect` (клиент может отключиться в любой момент многоходового стрима — раньше это ничем не было прикрыто); `AnalyzeRequest`/`StreamAnalyzeRequest` объединены через общий `_BaseAnalyzeRequest` с границами значений (`maxVisits`, `komi`, `boardXSize`/`boardYSize`, длина `turnNumbers` ≤ 1000) — добавлены regression-тесты в `test_schemas.py`; `main.py`'s `try/finally` расширен, чтобы покрывать `_find_free_port()`/`print()`.
- Frontend: `mapSgfRules` теперь `console.warn` на нераспознанный `RU` вместо тихого отката на chinese; `GTP_COLUMNS` вынесен в новый `board/gtpColumns.ts`, убрано дублирование между `BoardView.tsx`/`gameRequestBuilder.ts`; `getConnection()` в IPC-клиенте больше не кэширует отклонённый promise навсегда (сбрасывается на неудаче, следующий вызов ретраит); `sgfError`-сигнал в `App.tsx` экспортирован и добавлен в `afterEach` теста.
- Побочная находка при разборе `pnpm run lint`: красный статус был почти целиком (1900+ из ~1950 проблем) не унаследованными scaffold-правилами (как предполагалось раньше), а CRLF — на этой машине `core.autocrlf=true`, а в репозитории не было `.gitattributes`, поэтому весь чек-аут (backend и frontend одинаково) получал CRLF, а Prettier по умолчанию ожидает LF. Добавлен корневой `.gitattributes` (`* text=auto eol=lf`), рабочее дерево ренормализовано безопасно (сначала закоммичены все содержательные правки, затем `git rm --cached -r .` + `git reset --hard` — без риска, т.к. ничего незакоммиченного не было). Оставшиеся ~35 настоящих ошибок (`explicit-function-return-type`, `no-explicit-any` и т.п.) исправлены точечно в `src/` (добавлены явные типы возврата, `MessageEvent`/`CloseEvent` вместо `any` в `client.ts`) и ослаблены в `eslint.config.mjs` только для `tests/**` (моки/пустые заглушки там осознанно используют `any`/no-op — не признак качества кода, а идиома тестов). `pnpm run lint` теперь 0 ошибок/0 предупреждений; прогнан `eslint --fix` для оставшихся косметических prettier-warnings (только форматирование, поведение не менялось).
- Три коммита на ветке: (1) сами фиксы backend+frontend, (2) `.gitattributes`, (3) `style: eslint --fix`. Все 46 frontend + 42 backend теста и оба typecheck перепроверены зелёными после каждого шага.
- Обновлён `task_plan.md`: backlog-пункты вычеркнуты, добавлена запись в decisions log про `.gitattributes`/CRLF.

**Не сделано / решено не делать (осознанно отложено, без изменений):**
- Ownership-heatmap на партии с малым числом визитов — по-прежнему не трогали, см. `task_plan.md` backlog.
- Проверка совместимости React-only библиотек через `preact/compat` — по-прежнему "если/когда понадобится".
- Причина пропажи `uv` из PATH в части сессий — не расследовали, backend README уже документирует fallback.

**Открытые вопросы:** нет.

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Где мы сейчас».
2. Решить судьбу ветки `fix-phase-1-backlog-cleanup` (не смёржена на момент записи) — через `superpowers:finishing-a-development-branch`, если ещё не сделано.
3. Фаза 2 (LLM-объяснения) — не начинать без явного запроса пользователя.

## 2026-08-05/06 — Фаза 2, первый срез (`weak_group`-объяснение через Claude)

**Сделано:**
- Пользователь явно попросил начать Фазу 2. Полный объём Фазы 2 из `ARCHITECTURE.md` (несколько детекторов + calibration harness + 3 LLM-провайдера) — по сути несколько связанных подсистем; после явного вопроса пользователь решил разбить на под-этапы и указал, с чего начать: детектор `weak_group` (не `mistake`, не `opening_loss`), провайдер Claude (не Ollama/OpenAI), объяснение по клику (не автоматически на каждый ход), полная anti-hallucination защита, синхронный HTTP (не WS/streaming), переключатель вкладок «KataGo/LLM» в панели анализа (не отдельная секция).
- По ходу брейнсторминга пользователь поднял отдельную возможность — ручная SGF-разметка/комментарии с сохранением в файл (буквы/цифры/фигуры), никак не привязанную к LLM. Записана в backlog `task_plan.md`, начинается отдельным брейнстормингом **после** этого среза, не раньше — явное решение пользователя.
- Дизайн-спек — `docs/superpowers/specs/2026-08-05-phase-2-weak-group-explanation-design.md`, прошёл самопроверку и ревью пользователя без правок к самому спеку (только уточняющий вопрос про совместимость `/api/explain` с будущим ростом — ответ вписан в спек отдельным разделом).
- Implementation-план (8 задач) — `docs/superpowers/plans/2026-08-05-phase-2-weak-group-explanation.md`. Pre-flight сканирование плана перед диспетчем нашло и само исправило дублирование `_neighbors()` между Task 1/Task 2 (сделано публичной `neighbors()`, переиспользуется).
- Выполнено через `subagent-driven-development` на ветке `phase-2-llm-explanations`: Task 1 (union-find группы камней+дыхания), Task 2 (GTP-координаты+восстановление доски из ходов, с взятием), Task 3 (детектор `weak_group` — формулы `own_certainty`/`boundary_certainty`/`pv_focus`/`weak_score` дословно из спека; implementer сам нашёл и исправил IEEE-754 артефакт `0.4+0.3+0.2-0.05 != 0.85` через `round(score, 9)`, ревьюер независимо перепроверил), Task 4 (LLM-схемы+orchestrator+consistency checker с retry/fallback), Task 5 (Claude-провайдер, принудительный tool-use для structured output, добавлена зависимость `anthropic`), Task 6 (`POST /api/explain`, не трогает `EngineManager`/`engine_lock`, 503 на ошибку LLM), Task 7 (`explainPosition()` IPC-клиент), Task 8 (`AnalysisPanel`+`LlmExplanationPanel`, переключатель вкладок). Все 8 задач — ревью "Approved" на первом или втором прогоне; Task 8 прошёл 2 раунда фиксов (round 1: stale-результат при смене позиции без нового клика; round 2: гонка — устаревший in-flight-ответ на уже покинутую позицию всё равно долетал и перезаписывал состояние) — оба переверифицированы отдельным re-review.
- **Финальное whole-branch ревью (opus)** нашло межзадачный баг, невидимый ни в одном task-level review: `POST /api/explain` объявлен `async def`, но внутри синхронно вызывает Claude API — блокирует весь event loop backend'а (включая параллельный `WS /api/analyze/stream`) на время до 3 последовательных вызовов Claude. Плюс: `max_tokens=1024` при дефолтном adaptive thinking у Claude рисковал обрезать ответ до вызова `record_explanation` и уходить в необработанный 503 вместо спроектированного graceful fallback; `Finding` не хранил цвет группы и не давал GTP-координат — объяснение было не привязано к конкретной точке на доске, которую игрок может найти; `verified`-статус (ядро anti-hallucination дизайна) нигде не отображался в UI; `pnpm lint` регрессировал; новая обязательная `BADUK_CLAUDE_API_KEY` не задокументирована; `backend/uv.lock` не пересобран под `anthropic`.
- Один консолидированный fix-wave (не по фиксеру на находку) закрыл 9 из 10 находок, переверифицирован отдельным re-review — все ADDRESSED, ноль новых Critical/Important. `uv.lock` не исправлен — `uv` подтверждённо недоступен в этой среде (не только со слов реализатора — перепроверено самостоятельно). Re-review дополнительно нашёл одну новую minor-находку в самом фиксе (передан `boardXSize` вместо потенциально нужного `boardYSize` в `xy_to_gtp` — риск только на не квадратных досках, которых в проекте не бывает) — запаркована с обоснованием.
- Оба паркованных пункта (uv.lock, асимметричные доски) записаны в backlog `task_plan.md` с конкретным планом починки.
- Ветка `phase-2-llm-explanations` слита в `main` через `superpowers:finishing-a-development-branch` (fast-forward `d515c8d..2d23e52`, ветка и SDD-воркспейс удалены, тесты перепроверены на смёрженном `main`) и запушена в `origin` — оба действия сделаны по явному запросу пользователя, отдельными подтверждениями (сначала мёрж, потом отдельно пуш).

**Не сделано / решено не делать (осознанно отложено):**
- Калибровочный/backtesting harness, детекторы `mistake`/`opening_loss`, провайдеры OpenAI/Ollama, свободные вопросы к LLM, token-streaming объяснений, подсветка группы на доске — явно вне рамок этого среза, следующие под-этапы той же Фазы 2.
- Ручная SGF-разметка/комментарии — отдельная будущая задача, начинается после этого среза.
- `backend/uv.lock` не пересобран (см. backlog) — блокировано отсутствием `uv` в PATH на этой машине, не решение проекта.
- **Ручная сквозная приёмка с реальным Claude API — НЕ проводилась в этой сессии.** Все 130 тестов (73 backend + 57 frontend) прошли только с fake-провайдерами/моками; реальный вызов Claude, качество русскоязычного текста объяснения, корректность GTP-координат/цвета группы в промпте (добавлены только в fix-wave, никогда не виданы вживую) — ничего не проверено на живой партии. Это отличается от Фазы 1, где ручная приёмка была явным отдельным шагом (`fix-phase-1-acceptance-findings`) до того, как фаза считалась завершённой.

**Открытые вопросы:** нет по существу.

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Где мы сейчас»/backlog.
2. Ветка `phase-2-llm-explanations` уже смёржена в `main` и запушена в `origin` — не требует решения.
3. **Следующий логичный шаг — ручная приёмка Фазы 2/среза `weak_group` с реальным `BADUK_CLAUDE_API_KEY`** (по аналогии с ручной приёмкой Фазы 1), если пользователь захочет её провести — не начинать проактивно без запроса.
4. Дальше — либо продолжать под-этапы Фазы 2 (calibration harness, доп. детекторы/провайдеры, свободные вопросы), либо начать отдельную SGF-разметку, либо Фаза 3+ — только по явному запросу пользователя.

## 2026-08-06 — Ручная SGF-разметка/комментарии (дизайн + план + реализация, 9 задач)

**Сделано:**
- Пользователь явно попросил начать backlog-фичу «Ручная SGF-разметка/комментарии» (была отложена во время брейнсторминга Фазы 2). Полный брейнсторминг → дизайн-спек `docs/superpowers/specs/2026-08-06-manual-sgf-annotation-design.md`, прошёл самопроверку и явное одобрение пользователя без правок к тексту спеки.
- Ключевые дизайн-решения: разметка/комментарии — прямо как SGF-свойства узла через `tree.mutate()`, дерево партии остаётся единственным источником истины (не параллельный стор); сохранение — `sgf.stringify(tree.root)` (позже исправлено на `[tree.root]`, см. ниже); путь исходного файла — `webUtils.getPathForFile()`; тулбар инструментов разметки рядом с доской, клик по вершине применяет инструмент; пользовательская разметка всегда в приоритете над PV-метками KataGo в той же точке; третья вкладка «Разметка» с комментарием узла в существующей панели анализа; явная кнопка «Сохранить»/«Сохранить как» + предупреждение при закрытии с несохранёнными изменениями; редактирование дерева ходов и undo/redo — явно вне скоупа.
- Implementation-план (9 задач) — `docs/superpowers/plans/2026-08-06-manual-sgf-annotation.md`, с полным кодом на каждый шаг. Перед написанием плана лично перепроверены точные API сторонних библиотек в установленных `node_modules` (не по памяти): `@sabaki/sgf`'s `stringify`, `@sabaki/immutable-gametree`'s `Draft`, `@sabaki/shudan`'s `Marker`-тип и `onVertexClick`, уже готовый `window.electron.webUtils` через `@electron-toolkit/preload`.
- Выполнено через `subagent-driven-development` на ветке `feature-sgf-annotation` (от `main`, форк `d8c722b`): Task 1 (`board/annotations.ts` — мутация/чтение разметки), Task 2 (`board/sgfSerializer.ts`), Task 3 (`currentFilePath`/`isDirty`/`currentNode` в `appState.ts`), Task 4 (рендер существующей разметки на доске с приоритетом над PV), Task 5 (тулбар + клик-по-доске), Task 6 (вкладка «Разметка» с комментарием), Task 7 (main-process file-save IPC, `fileIO.ts`), Task 8 (UI сохранения в `App.tsx`, захват пути при drag&drop), Task 9 (предупреждение при закрытии окна).
- **Три раунда точечных фиксов (Tasks 2, 5, 6) — каждый раз реальный баг в самом плане, не в реализации субагента**, каждый раз подтверждён у пользователя перед фиксом (правило: находка, противоречащая буквальному тексту уже одобренного плана, — решение человека, не контроллера): (1) Task 2 — `sgf.stringify(tree.root)` без обёртки в массив давал SGF без внешних скобок, невалидный по спецификации (проверено лично в Node против установленного пакета) — исправлено на `stringify([tree.root])`; (2) Task 5 — implementer столкнулся с внутренней нестыковкой плана (тест тулбара рендерил `<BoardView/>` без загруженной партии, но код по плану помещал тулбар только в ветку с загруженной партией) и сам решил её, отрендерив тулбар в обеих ветках — ревью признало это неверным решением (реальный footgun: инструмент разметки мог «вооружиться» до открытия файла и пережить загрузку), правильный фикс — тулбар только в загруженном состоянии + починка теста по образцу соседних; (3) Task 6 — `handleBlur` коммитил изменение в дерево на каждый выход фокуса из поля комментария, даже без реальной правки текста, что противоречило собственной логике плана «commit только на изменение» — исправлено сравнением с загруженным значением через ref; заодно исправлена и вторая находка того же ревью — компонент дублировал логику уже существующего `currentNode` вместо использования его.
- **Финальное whole-branch ревью (opus)** нашло 1 Critical + 5 Important, ни одна не могла всплыть на task-level review: (a) накопленный за несколько задач lint-долг (2 реальные ESLint-ошибки — невалидный triple-slash pragma в тестовом файле, неиспользуемый импорт — плюс 15 предупреждений форматирования) не проверялся построчно ни одним ревьюером до конца, `pnpm run lint` падал с ненулевым кодом; (b) комментарий узла «залипал» после загрузки второго файла — `parseSgf` сбрасывает счётчик id на каждый разбор, поэтому корневые узлы разных партий получают одинаковый numeric id, из-за чего эффект `AnnotationPanel`, завязанный на `[nodeId]`, не перезапускался (Task 6 писал эффект, Task 8 владел `loadGame` — виден только на пересечении); (c) текст комментария молча терялся при навигации колёсиком мыши по дереву вариаций (в отличие от клика, не убирает фокус — commit-on-blur не срабатывал); (d) краш рендера доски на разметке с координатами вне текущего размера доски из стороннего SGF — нет bounds-check, актуально именно потому что чтение чужой разметки — заявленная фича; (e) перезапись файла в не-UTF8 кодировке (`CA[Shift_JIS]`/`CA[windows-1251]` и т.п.) могла молча испортить партию — пользователь явно выбрал «предупреждать и отказывать», не пытаться полноценно поддержать другие кодировки; (f) неатомарная запись файла (риск битого файла при сбое на середине записи); (g) выход через Cmd+Q/`app.quit()` убивал backend раньше, чем пользователь успевал отменить диалог о несохранённых изменениях — унаследованный из Фазы 1 `before-quit`-хендлер не знал про новый `close`-хендлер Task 9. Плюс 8 Minor.
- По явному решению пользователя все 17 находок (Critical+Important+все Minor) отправлены одним consolidated fix-wave, не по раундам. Fix-wave (commit `cdc5c43`) исправил всё; scoped re-review подтвердил все 17 ADDRESSED, ноль новых Critical/Important — один Minor запаркован (осиротевший `.tmp`-файл при неудачном `rename()`, не рискует потерей данных, не блокирует). Особое внимание re-review уделил повторной трассировке ре-энтерабельности фикса №(c) (коммит-при-навигации синхронно перезапускает тот же signal-эффект) — вручную прослежено, что нет ни бесконечного цикла, ни двойного коммита, ни молчаливого no-op.
- Все 18 файлов / 118 тестов, typecheck (оба процесса), `pnpm run lint` (0 ошибок, 0 предупреждений) зелёные на итоговом коммите.

**Ручная сквозная приёмка — проведена и подтверждена пользователем в этой же сессии, сразу после мёржа.** Пользователь дал реальные пути (`.katrain` opencl-сборка KataGo + модель) и попросил провести тест самостоятельно. Backend поднят с реальным KataGo + фиктивным `BADUK_CLAUDE_API_KEY` (fail-fast проверяет только непустоту строки, реального вызова Claude функция разметки не делает вообще). Приложение запущено через `electron-vite dev` с `BADUK_DEBUG_PORT`, управлялось через Chrome DevTools Protocol напрямую по WebSocket (без готовых библиотек — Node 22+ имеет глобальный `WebSocket`). По пути нашлась и решилась инфраструктурная проблема этой конкретной песочницы: `ELECTRON_RUN_AS_NODE=1` был выставлен глобально в окружении инструмента, из-за чего Electron запускался как обычный Node-процесс без `app`/`BrowserWindow` — решено через `unset` перед запуском дочернего процесса. Драг-дроп файла симулирован через `Input.dispatchDragEvent` (CDP) с реальным путём на диске (не JS `Blob`/`File`) — это принципиально, т.к. `webUtils.getPathForFile()` не резолвит путь для не-дисковых `File`-объектов; настоящий CDP drag-drop это условие удовлетворяет. Подтверждено вживую: открытие чужого SGF с разметкой (включая пограничный случай «фигура+метка в одной точке на доске из внешнего файла» — метка корректно победила, как и задокументировано в Task 1 review), расстановка новой фигуры + правка комментария (индикатор «\*» появился), «Сохранить» реально переписал файл на диске (проверено чтением файла до/после — валидный SGF с внешними скобками, новая фигура добавлена в существующий `TR[...]`, комментарий обновлён), переоткрытие сохранённого файла показало оба изменения. Два сценария с нативными Windows-диалогами («Сохранить как», предупреждение при закрытии) CDP кликнуть не может (native-диалоги — отдельные HWND-окна вне поверхности рендеринга Chromium) — их проверил лично пользователь в живом окне, подтвердил, что всё работает.
- Ветка `feature-sgf-annotation` смёржена в `main` (fast-forward `d8c722b..40aeef1`, ветка и SDD-воркспейс удалены). **Не запушена** в `origin` — отдельное решение, ещё не запрошено пользователем.
- По итогам приёмки пользователь сразу предложил новую небольшую фичу: визуально показывать в дереве вариаций, у каких узлов есть комментарий/разметка (например, иконка «треугольник в кружке»). Ещё не спроектирована — начинается отдельным брейнстормингом в этой же сессии.

**Открытые вопросы:** нет по фиче SGF-разметки — полностью принята.

## 2026-08-06 (продолжение) — Индикатор комментария/разметки в дереве вариаций (дизайн + план + реализация)

**Сделано:**
- Полный брейнсторминг мини-фичи, предложенной пользователем сразу после приёмки SGF-разметки: маленький зелёный треугольник внутри маркера узла дерева вариаций для узлов с комментарием и/или разметкой. Дизайн-спек прошёл самопроверку и одобрение пользователя без правок: `docs/superpowers/specs/2026-08-06-variation-tree-annotation-indicator-design.md`.
- Implementation-план (1 задача, полностью специфицирован кодом) — `docs/superpowers/plans/2026-08-06-variation-tree-annotation-indicator.md`.
- Выполнено через `subagent-driven-development` на ветке `fix-variation-tree-annotation-indicator` (от `main`, форк `c76b360`): `hasAnnotation(node)` + рендер SVG-иконки в `VariationTree.tsx` (commit `cfe0b42`), task-level review — чисто с первого раза.
- **Финальное whole-branch ревью (opus)** нашло 2 Important (оба — пробелы тестового покрытия: ассерты не были привязаны к конкретному узлу через `aria-label`, баг «иконка не на том узле» прошёл бы незамеченным; не было теста на корневой узел, хотя спек явно требует его поддержки) + 4 Minor (дублирование списка ключей разметки между `annotations.ts`/`VariationTree.tsx`; смещение иконки на ~1px от центра — но точные px-значения взяты дословно из уже одобренного плана, так что правка требовала бы отступить от буквального текста плана, значит — решение пользователя, не контроллера; отсутствие a11y-подписи на иконке; не обновлённые `task_plan.md`/`progress.md`).
- Пользователю заданы вопросы: 2 Important — чинить без вопросов (не спорные, не конфликтуют с планом); по Minor — какие включить в тот же фикс-вейв. Пользователь выбрал только дедупликацию ключей разметки; смещение иконки и a11y-подпись оставлены как есть.
- Один фикс-вейв (commit `58e905f`): тесты теперь скоуплены через `[aria-label="..."] .variation-tree__marker-annotation-icon` + проверка count; добавлен тест на корневой узел; `FIGURE_PROPERTIES` экспортирован из `annotations.ts` и переиспользован в `VariationTree.tsx` вместо параллельного литерала. Scoped re-review подтвердил все 3 находки ADDRESSED, новых Critical/Important не найдено.
- Все 123 теста, оба typecheck, `pnpm run lint` — зелёные на итоговом коммите `58e905f`.

**Ветка `fix-variation-tree-annotation-indicator` смёржена в `main`** (fast-forward `c76b360..c2e225f`, ветка и SDD-воркспейс удалены, тесты перепроверены на смёрженном `main`). По явному запросу пользователя ("запушь") `main` также **запушен в `origin`** (`0702ae2..c2e225f`) — это включало и уже смёрженную ранее `feature-sgf-annotation`.

**Открытые вопросы:** нет по этой фиче — полностью смёржена и запушена.

## 2026-08-06 (продолжение) — Gemini как второй LLM-провайдер (дизайн + план + реализация)

**Сделано:**
- После пуша пользователь спросил "что дальше по плану" — предложены варианты из backlog (ручная приёмка Фазы 2 с реальным Claude, остальной объём Фазы 2, Фаза 3+, мелкий backlog). Пользователь явно попросил вместо этого начать новую задачу — подключение LLM через Google Gemini (мотивация: нет ключа Claude, есть доступ к Gemini — тот же мотив, что уже звучал раньше в сессии при обсуждении ручного теста SGF-разметки, тогда осознанно отложенный).
- Полный брейнсторминг: исследована текущая архитектура (`LLMProvider`-протокол уже провайдер-агностичен, только `main.py::run()` жёстко создавал `ClaudeProvider`). Пользователь на отдельный уточняющий вопрос "зачем новый провайдер, не унифицированный" — рассмотрена и явно отклонена унификация через LiteLLM (риск для anti-hallucination forced tool-use гарантии, YAGNI при двух провайдерах). Дизайн-спек: `docs/superpowers/specs/2026-08-06-gemini-llm-provider-design.md` — два отдельных провайдер-класса на официальных SDK, общий `llm/prompts.py` для анти-галлюцинационного текста, выбор через `BADUK_LLM_PROVIDER` (дефолт `"gemini"` — явное решение пользователя).
- Перед написанием плана лично верифицирована точная API `google-genai` v2.17.0 интроспекцией установленного пакета (не по памяти) — `genai.Client`, `generate_content`, `GenerateContentConfig`/`ToolConfig`/`FunctionCallingConfig`/`ThinkingConfig`, `FunctionDeclaration.parameters` принимает голый JSON-schema dict, `response.function_calls` — готовое SDK-свойство. Модель по умолчанию `gemini-3.6-flash` выбрана через веб-поиск (стабильна на август 2026, бесплатный тариф; `gemini-2.5-flash` уходит под deprecation 16 октября 2026).
- Implementation-план (3 задачи) — `docs/superpowers/plans/2026-08-06-gemini-llm-provider.md`, выполнен через `subagent-driven-development` на ветке `phase-2-gemini-provider` (от `main`, форк `7a62cb5`): Task 1 (вынесение `SYSTEM_PROMPT`/`build_user_prompt`/схемы в `llm/prompts.py`, рефакторинг `claude.py` — регрессионная сеть, существующие тесты не менялись), Task 2 (`GeminiProvider` + юнит-тесты на фейковом клиенте + самоскипающийся интеграционный тест), Task 3 (`_select_llm_provider()` в `main.py`, README/`task_plan.md` backlog обновлены). Все три задачи прошли task-level review чисто с первого раза.
- **Финальное whole-branch ревью (opus)** нашло 1 Critical (`thinking_budget=0` — параметр линейки Gemini 2.5, дефолтная модель `gemini-3.6-flash` — линейка Gemini 3, которой нужен `thinking_level`, иначе риск 400-ошибки на каждом вызове дефолтного провайдера — независимо перепроверено веб-поиском и интроспекцией установленного SDK: `ThinkingLevel` содержит `MINIMAL`/`LOW`/`MEDIUM`/`HIGH`) + 2 Important (устаревший `CLAUDE.md` всё ещё требовал `BADUK_CLAUDE_API_KEY` безусловно; обязательная по плану ручная проверка на живом API не выполнена) + 4 Minor.
- Critical-находка отступала от буквального текста уже одобренного плана (план мандатировал `thinking_budget=0`) — по установленному в сессии правилу подтверждена у пользователя перед фиксом (единственный разумный путь, вопрос задан без второй опции). Один фикс-вейв закрыл Critical (`thinking_level="MINIMAL"`) + `CLAUDE.md` + один Minor по выбору пользователя (`call.args=None` давал `ValidationError` вместо `RuntimeError`); `uv.lock`/`max_output_tokens`/`vertexai=False` осознанно отложены. Scoped re-review подтвердил все 3 находки ADDRESSED, ноль новой поломки — независимо перепроверил валидность `thinking_level="MINIMAL"` против установленного SDK, а не поверил диффу на слово.
- **Живая проверка на реальном API**: пользователь предоставил настоящий `BADUK_GEMINI_API_KEY`. Тест дошёл до Gemini API и корректно разобрал структурированный ответ — но получил `400 FAILED_PRECONDITION: User location is not supported for the API use` (гео-ограничение аккаунта/сети на стороне Google, не баг в коде) вместо успеха. Полный успешный round-trip не подтверждён. Пользователю задан прямой вопрос о мерже в таком состоянии — явно одобрил "мержим сейчас, без полного round-trip".
- Все 85 backend-тестов (полный non-integration suite), typecheck — зелёные на итоговом коммите `4a6cfbb`.

**Открытые вопросы:** Gemini-путь `/api/explain` статически и schema-корректен, но ни разу не подтверждён успешным вызовом вживую — см. backlog в `task_plan.md`.

**Ветка `phase-2-gemini-provider` смёржена в `main`** (fast-forward `7a62cb5..4532b09`, тесты перепроверены на смёрженном дереве, ветка удалена) и **запушена в `origin`** (`0702ae2..c2e225f`, по явному запросу пользователя "запушь").

Сразу после мержа пользователь спросил "что дальше по плану" (см. backlog-варианты выше), но вместо любого из них явно попросил новую задачу — переход на `llama-cpp-python` (см. следующую секцию).

## 2026-08-06 (продолжение) — llama-cpp-python как третий LLM-провайдер (дизайн + план + реализация)

**Сделано:**
- Пользователь после мержа Gemini явно поднял проблему: платные облачные API с лимитами запросов, попросил спроектировать переход на `llama-cpp-python`. На вопрос "третий провайдер или полная замена" пользователь сначала спросил "какие локальные модели подойдут" — отдельный веб-поиск (август 2026) дал рекомендацию `Qwen3-8B` GGUF Q4_K_M (~5 ГБ, `unsloth/Qwen3-8B-GGUF`) под NVIDIA-карту ≤8 ГБ VRAM (уточнено отдельным вопросом — та же карта, что уже гоняет KataGo, opencl-сборка), только после этого подтверждён выбор "третий провайдер, дефолт не меняется".
- Дизайн-спек `docs/superpowers/specs/2026-08-06-llama-cpp-provider-design.md`: третий провайдер на официальном `llama-cpp-python`, grammar-constrained JSON через `response_format={"type":"json_object","schema":...}` вместо forced tool-use — механически даже более строгая гарантия structured output (проверено лично: `LlamaGrammar.from_json_schema` реально конвертирует `EXPLANATION_TOOL_PARAMETERS` в GBNF с жёстко зафиксированными required-ключами и enum). Конфигурация модели через `BADUK_LLAMA_MODEL_PATH`/`BADUK_LLAMA_N_GPU_LAYERS` env var (тот же паттерн, что `BADUK_KATAGO_BINARY`/`BADUK_KATAGO_MODEL` — путь не коммитится, не скачивается автоматически).
- Перед написанием плана лично верифицирован точный API `llama-cpp-python` v0.3.34 по исходникам на GitHub (не по памяти) — критично: библиотечные дефолты не подходят проекту без явного переопределения (`n_gpu_layers=0`, а не `-1`; `n_ctx=512`, а не 8192). Также лично воспроизведён Windows long-path build fail при `pip install llama-cpp-python` на этой машине (нет готового wheel под текущую платформу) — задокументирован в плане с обходным путём (готовый CUDA-wheel как основной маршрут установки).
- Implementation-план (2 задачи) — `docs/superpowers/plans/2026-08-06-llama-cpp-provider.md`, выполнен через `subagent-driven-development` на ветке `phase-2-llama-provider` (от `main`, форк `7ba80cd`): Task 1 (`LlamaProvider` в `llm/providers/llama.py` + юнит-тесты на fake-объекте без реальной модели/GPU + самоскипающийся интеграционный тест), Task 2 (`_select_llm_provider()` третья ветка `"llama"`, README с рекомендуемой моделью и установкой CUDA). Task 2 прошёл 1 раунд фикса — тест падал в изоляции (`TypeError` от eager-вычисления аннотации `llama_cpp.Llama | None` при монкипатче до первого импорта модуля, из-за отсутствия `from __future__ import annotations`), исправлено и живо переверифицировано (тест прошёл в изоляции).
- **Финальное whole-branch ревью (opus)** нашло 3 Important, ни один не мог всплыть на task-level review: (1) `llama-cpp-python` был обязательной зависимостью — на машине без подходящего wheel (эта самая машина) ломался сбор ВСЕГО тест-сьюта, а не только llama-тестов; (2) единственный `Llama`-инстанс не был защищён локом при параллельных `/api/explain` (через `asyncio.to_thread`) — `llama_cpp.Llama` не потокобезопасен (подтверждено grep'ом по SDK), риск порчи состояния/segfault всего backend-процесса; проект уже решал эту же задачу для KataGo (`engine_lock`), здесь прецедент не применён; (3) README не предупреждал о конкуренции за VRAM с KataGo на той же ≤8-ГБ карте — риск OOM, который бы выглядел как сбой именно KataGo (стартует вторым), а не LLM. Плюс 4 Minor.
- Фикс-вейв (commit `3d227c2`) закрыл все 3 Important + 3 из 4 Minor по выбору пользователя (README-имя модели, невнятная ошибка на мусор в `BADUK_LLAMA_N_GPU_LAYERS`, `finish_reason`+content excerpt в текст ошибки). Re-review нашёл residual gap в фиксе Important 1: `main.py`'s `"llama"`-ветка всё ещё импортировала `LlamaProvider` до проверки `BADUK_LLAMA_MODEL_PATH` (тот же import-then-check порядок, что в plan-мандатированных claude/gemini-ветках, но там безопасно — их SDK не имеют этой хрупкости). Подтверждено у пользователя перед фиксом (отступление от буквального текста плана), исправлено перестановкой порядка (commit `b54ee4f`), живо переверифицировано повторным запуском ранее падавшего теста в изоляции.
- **Живая проверка на реальной модели**: пользователь предоставил путь к скачанной `Qwen3-8B-Q4_K_M.gguf`. `test_explain_with_real_llama` — **PASSED** за 123с (CPU-инференс — CUDA-сборка `llama-cpp-python` не загрузилась на этой машине, нет CUDA Toolkit runtime, только драйвер NVIDIA), непустой schema-конформный `Explanation`. Первый подтверждённый полный round-trip среди всех трёх LLM-провайдеров в этой сессии (Claude ещё не проверялся вживую, Gemini упёрся в гео-ограничение аккаунта). GPU-оффлоад остаётся неподтверждённым (backlog).
- Все 97 backend-тестов (полный non-integration suite) — зелёные на итоговом коммите `b54ee4f`.

**Открытые вопросы:** GPU-оффлоад `llama-cpp-python`-провайдера ни разу не проверялся живьём (только CPU-инференс на этой машине) — см. backlog в `task_plan.md`. Решение о мерже ветки `phase-2-llama-provider` в `main` — следующий шаг сразу после этой записи.

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Где мы сейчас».
2. Если ветка `phase-2-llama-provider` не была смёржена в этой сессии — предложить/уточнить в начале следующей.
3. Если появится доступ к машине/окружению с рабочим CUDA Toolkit — прогнать реальный GPU-инференс и закрыть открытый backlog-пункт (для обоих провайдеров — Gemini гео-ограничение и llama GPU-оффлоад).
4. Дальше — либо доводить под-этапы Фазы 2 (ручная приёмка с Claude, доп. детекторы), либо Фаза 3+, либо что-то новое — только по явному запросу пользователя.

## 2026-08-11 — llama default provider (продолжение с прошлой сессии), уборка процессов, Vulkan GPU-оффлоад, детектор `mistake`

**Сделано (несколько отдельных запросов пользователя за сессию, каждый со своим брейнстормом):**

- **`fix-llama-default-provider`** смёржена в `main` (fast-forward `91cf279..2c56e6c`) — дефолт `BADUK_LLM_PROVIDER` сменён на `"llama"`, Claude/Gemini код нетронут. Затем по запросу пользователя убраны из `task_plan.md` два уже неактуальных backlog-пункта («ручная приёмка Фазы 2 с реальным Claude API», «Gemini live round-trip не подтверждён») — они звучали как открытые задачи, хотя пользователь явно решил переключить фокус на llama.
- **Утечка процессов**: по запросу пользователя проверены и остановлены 8 зависших `python.exe` (backend-сервис) + 69 `node.exe` (тестовый fake-backend), висевших с 4–6 августа (idle, 0% CPU, KataGo среди них не было). Найдена находка о причине (тесты/integration-прогоны не убивают дочерний процесс штатно), задокументирована в backlog `task_plan.md`, сама причина не чинилась (не просили).
- **Vulkan GPU-оффлоад для llama-cpp-python**: по запросу пользователя проверена работоспособность GPU-оффлоада (открытый backlog-пункт). CUDA-wheel по-прежнему не грузится на этой машине (нет CUDA Toolkit, только драйвер). Установлена prebuilt Vulkan-сборка (`--extra-index-url .../whl/vulkan`) — не требует CUDA Toolkit, использует `vulkan-1.dll` из драйвера GPU. Логи подтвердили `load_tensors: offloaded 37/37 layers to GPU` на `Vulkan1 (RTX 5060 Ti)`, `nvidia-smi` показал рост VRAM до ~5.4 ГБ и до 95% `utilization.gpu` во время генерации, ~29.7 ток/с (vs ~123с на CPU). Ветка `fix-llama-vulkan-docs` (README-доку­ментация Vulkan как альтернативы CUDA) смёржена и запушена.
- **Детектор `mistake` (второй под-этап Фазы 2)** — пользователь явно попросил продолжить «остаток Фазы 2» (после уточнения — RAG или Фаза 2, выбрал Фазу 2), затем выбрал начать с доп. детекторов из трёх независимых частей (детекторы / калибровка / свободные вопросы). Брейнсторминг раскрыл нетривиальный технический вопрос — как считать цену хода без риска молчаливого провала на самых грубых ошибках (см. `task_plan.md` decisions log 2026-08-11 за детали формулы/знака/порогов). По запросу пользователя проверен его личный репозиторий `Baduk-knowledge-base` как источник калибровочных данных — не подошёл (чисто качественный контент), зато отмечен как готовый источник для будущей Фазы 3.
  - Implementation-план (5 задач) выполнен через `subagent-driven-development` на ветке `phase-2-mistake-detector` (форк `ab15c21`). Task 1 — 1 раунд фикса (пропущенный файл с прямой конструкцией `Finding(...)`, найден ревьюером, один Minor о качестве репорта адъюдицирован контроллером). Task 3 — implementer сам нашёл ещё один пробел плана (`llm/schemas.py`) и корректно его закрыл. Task 4 — обнаружена и исправлена реальная ошибка знака в собственной тестовой фикстуре плана (моей), подтверждена ре-ревьюером вручную.
  - **Финальное whole-branch ревью (opus)**: 2 Critical + 4 Important + 5 Minor — детали в `task_plan.md` decisions log. Самое серьёзное: общий на оба типа находок enum `cited_field` при per-type `_FINDING_FIELDS` в `consistency.py` — цитата моделью «чужого» поля не ловилась ни одним task-level review (каждый тестировал только корректно типизированные claims) и превращалась в необработанный `AttributeError` → HTTP 503, **на уже существующем `weak_group`-пути**, не только новом. Один консолидированный фикс-вейв + один scoped re-review закрыли все 6 code-level находок, независимо перепроверенных (включая свежий repro-скрипт для Critical-бага). Три планинг-файла проекта (тоже Critical-находка ревью, процессная) обновлены контроллером напрямую в этой же записи, а не через субагента.
  - Все 122 backend + 125 frontend теста, оба typecheck — зелёные. Ветка готова к мержу, решение о мерже/пуше ещё не запрошено у пользователя (следующий шаг сразу после этой записи).

**Открытые вопросы:** решение о мерже `phase-2-mistake-detector` — следующий шаг. Три Minor-находки финального ревью отложены в backlog `task_plan.md` (тест на fallback mistake→weak_group при Δ ниже порога, тест на вторую сторону both-or-neither валидатора, страховочный тест на синхронность трёх копий списка `cited_field`).

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Где мы сейчас» и backlog.
2. Если `phase-2-mistake-detector` не была смёржена/запушена в этой сессии — уточнить у пользователя в начале следующей.
3. Дальше — `opening_loss`-детектор (естественное продолжение `mistake`), или калибровка/harness, или свободные вопросы к LLM (все три — независимые под-этапы, ещё не начаты), либо Фаза 3+, либо что-то новое — только по явному запросу пользователя.

## 2026-08-11 (продолжение) — Фаза 3, первый под-этап: RAG ingestion + retrieval

**Сделано:**

- Пользователь явно попросил начать Фазу 3. Брейнсторминг: контент — не сбор с нуля из открытых источников (как изначально писал `ARCHITECTURE.md`), а готовый личный репозиторий пользователя `Baduk-knowledge-base` (452 карточки Принцип/Ошибка/Упражнение); индексируются все три типа как независимые плоские чанки (одна карточка = один чанк); реальные категории базы заменяют иллюстративные из `ARCHITECTURE.md`; эмбеддинги — BGE-M3 локально на CPU (тот же local-first принцип, что и решение сделать llama-cpp-python дефолтным LLM-провайдером); векторное хранилище — Chroma; индексация — полная пересборка по запросу, без инкрементальной синхронизации; срез явно ограничен ingestion+retrieval, без LLM-пайплайна/frontend (следующий под-этап). Дизайн-спек `docs/superpowers/specs/2026-08-11-phase-3-rag-ingestion-design.md` одобрен пользователем без правок («норм»).
- Implementation-план (3 задачи) написан и самостоятельно провалидирован (при самопроверке лично поймал и исправил: избыточный `pytest.raises` в тесте Task 2 и потенциальную проблему numpy-scalar-float vs Chroma `embeddings=` — оба зафиксированы в самом плане до начала выполнения).
- Выполнен через `subagent-driven-development` на ветке `phase-3-rag-ingestion` (форк `70f7715`):
  - **Task 1** (парсер карточек, haiku) — approved с первого раза, 2 Minor отложены (non-dict YAML guard, тест на полностью отсутствующий frontmatter-блок).
  - **Task 2** (ingestion-скрипт, sonnet) — 1 раунд фикса на 2 Important (голый `except Exception` вокруг `delete_collection` вместо узкого типа; тест «full rebuild» проверял только стабильность счётчика на неизменном вводе, что прошло бы и при upsert-in-place) — оба однозначно резолвлены контроллером без вопроса пользователю (не архитектурные развилки, а дыры в качестве кода/тестов), ре-ревью подтвердил ADDRESSED с независимой эмпирической проверкой (включая временный revert-and-retest, доказавший, что усиленный тест реально ловит регрессию на upsert).
  - **Task 3** (`retrieve_knowledge()` + integration-тест, haiku) — approved с первого раза (3 Minor отложены).
  - Ревью-агент Task 3 прервался на середине из-за упора в сессионный лимит API — пользователь написал «продолжай», агент дорезюмирован через `SendMessage` с тем же контекстом и корректно завершил обзор.
- **Финальное whole-branch ревью (opus)**: 0 Critical, 6 Important (все воспроизведены живьём — вкл. полный прогон парсера по всем 452 реальным карточкам базы), 10 Minor. Самое существенное: `backend/rag_store/` не был добавлен в `.gitignore` (прямое нарушение уже одобренного спека); `test_cards.py` не имел `pytest.importorskip("yaml")` — на чистой установке без `[rag]`-экстры 4 теста падали с ошибкой, а не скипались (нарушение критерия готовности спека); эмбеддинг-модель не была явно закреплена на CPU (`device="cpu"`) — на этой машине спасал только случайно CPU-only torch; опечатка в `BADUK_KNOWLEDGE_BASE_PATH` молча уничтожала существующий индекс и репортила успех (`Path.glob()` на несуществующей директории просто возвращает `[]`); главный юнит-тест `retrieve_knowledge()` был вырожденным (`len(snippets) <= 2` проходит и на пустом списке); `README.md` не документировал новую env var/установку/команду ingestion. Ревьюер прямо отметил, что 4 из 6 Important — дефекты самого плана (конфликт Global Constraint с уже одобренным спеком, потерянное при переходе спек→план требование CPU, вырожденный ассерт в самом плане, отсутствие `importorskip` в собственном коде плана), а не самодеятельность имплементеров.
- Все 6 Important + 4 отобранных ревьюером Minor (сузить except в `retrieval.py`, вынести дублирующуюся float-коэрцию в общий `to_float_vectors()` в `store.py`, non-dict YAML guard, тест на отсутствующий frontmatter) закрыты одним консолидированным фикс-вейвом контроллером — без обращения к пользователю, т.к. ни один не противоречил реальным дизайн-решениям с брейнсторминга (BGE-M3/Chroma/full-rebuild/`RagSnippet`-схема), только приводил реализацию к уже одобренному спеку. Scoped re-review подтвердил все 10 находок ADDRESSED, включая поведенческую проверку (monkeypatch, доказавший, что оба новых guard'а в `ingest.py` срабатывают до любого обращения к Chroma). Новых находок нет. **READY TO MERGE.**
- Оставлены в backlog (не входили в рекомендованный ревьюером фикс-вейв): `@lru_cache` на `get_embedding_model()` (актуально станет только когда `retrieve_knowledge` начнёт вызываться из `/api/explain` в следующем под-этапе), предупреждение в лог на карточки длиннее контекстного окна BGE-M3 (затронута ровно 1 из 452 карточек), детекция коллизии `doc_id` между `principles`/`mistakes`/`exercises` (коллизий сейчас нет, риск латентный), устаревшее описание pytest-маркера `integration` в `pyproject.toml` (уже относится не только к KataGo).
- Все 131 backend-теста (6 integration корректно отсеяны по умолчанию) — зелёные на итоговом коммите `da2724f`.

- Ветка смёржена в `main` (fast-forward `70f7715..71171db`), тесты перепроверены на смёрженном дереве (131 passed). Не запушена в `origin` — `main` сейчас на 12 коммитов впереди `origin/main`, отдельное решение, ещё не запрошено.
- **Живая проверка проведена**: пользователь выбрал её как следующий шаг после мержа. Реальный `BADUK_KNOWLEDGE_BASE_PATH` (личный репозиторий `Baduk-knowledge-base`) + `test_ingest_and_retrieve_real_knowledge_base` — **PASSED** за 27 минут (в основном первая загрузка BGE-M3 ~2.3 ГБ + CPU-эмбеддинг всех 452 карточек). `run_ingest()` реально прошёл по всей базе и записал в Chroma (`backend/rag_store/`, ~16 МБ на диске, подтверждено гитигнорится — `git status` его не видит). `retrieve_knowledge("два глаза необходимы для безусловной жизни группы", top_k=3)` вернул `two-eyes-necessary-for-unconditional-life` в top-3 — критерий готовности спека подтверждён вживую, не только на фейковой embedding-модели из юнит-тестов.

**Открытые вопросы:** нет открытых по Фазе 3, первому под-этапу — реализован, смёржен, живьём проверен. Пуш `main` в `origin` — отдельное нерешённое.

**Следующая сессия начинает с:**
1. Прочитать `task_plan.md` → «Где мы сейчас» и backlog.
2. Дальше — следующий под-этап Фазы 3 (подключение `retrieve_knowledge` к LLM-пайплайну, см. «Заметка на будущее» в дизайн-спеке), либо `opening_loss`-детектор/калибровка Фазы 2, либо пуш `main` в `origin`, либо что-то новое — только по явному запросу пользователя.
